# r11c 底稿 · 片 F —— 装订前状态盘点(只报不改)

> **本片性质:只报不改。** 不动 `chapters/` 一个字,不动任何历史文件。
> 产出为三份供 R12 装订直接执行的清单 + 配套 TSV。
> 溯源约定:对 hermes-agent 的断言用 `路径:行号 @ 863e313`;
> 对本仓库产出(`chapters/`、`notes/`、`reports/`)的定位用 `文件:行号`,
> 它们不在基线里,校验器不比对(见 §0 的口径说明)。

**任务范围(派工书「片 F」三件事)**

1. 成品章清单与章序建议 + 章号重号/跳号核对;
2. 跨章重复条目(判据 + 可重跑命令 + 建议保留位置);
3. 结论已被后续轮次更新、而成品章正文未同步的位置(逐条给 `文件:行号`、原结论、后续改判、出处锚点)。

**独立性声明**:第 3 项与片 E 有重叠,按派工书要求**两片独立做**。
本片从「成品章现状」出发反查;**没有读过、也没有引用片 E 的任何产出**
(片 E 在本批次尚未派发,`notes/r11c-raw-reversal-propagation.md` 在本片收工时不存在,
见 §4 开头的实测)。

---

## 0. 结论速览(先结论后论证)

1. **成品章是 21 份不是 20 份**,合计 12,555 行。章号 1–21 连续、无内容断档。
   建议 R12 **保序装订 + 加六个分部**,不重排——正文里有 6 处写死的章号交叉引用,
   而**没有任何关卡校验章号**。(§1)
2. **章号有 1 处重号,就在成品章正文里**:`chapters/r11b-the-unwritten-layer.md:255`
   把 `r7b-platform-integration.md` 叫作第十一章,而第十一章是 `r8b-cli-trunk-and-interaction.md`;
   r7b 是第八章。另有 7 个章号(3/4/5/6/8/9/10)**从未在任何地方被写出来过**,
   只活在 `reports/round-9d-l1-completion.md` §8.2 表的序号列里。(§2)
3. **跨章重复**:四条判据给出候选 22(同一处代码)+ 58(术语)+ 0(事故)+ 3(定案);
   逐簇读正文后判定 **8 组 R12 必须合并**、9 组是有意的交叉引用、其余是同文件邻近处的不同断言。
   另有 1 条不是重复而是**冲突**:同一个覆盖率被第 13 章与第 16 章用两种口径各报一次,
   谁都没标口径。(§3)
4. **成品章里未同步的过期结论确认 4 条**。最严重的一条是
   `chapters/r1-what-is-hermes-agent.md:98-123`:那段自称「下表是当前值」的分层表与进度数字
   **整段过期**(L1 写 511/479,923,现为 563/522,207;`R1-inventoried` 写 8,122 文件,现为 5,944),
   而**同一处在 R8-fix 已经被修过一次**,修法选的是「换一份新手抄件」,于是六轮后原样复发。(§4.1)
5. **一个方法论结论**:第 4 条那种最严重的过期,**三条机械判据一条都没抓到**——
   它们全都在找「章内说法 vs 后轮语料说法」的矛盾,而这一条是「章内数字 vs
   本仓库另一份可计算数据」的矛盾。这类检查目前是空白,移交 `H-R11C-F-b`。(§4.6)

---

## 1. 成品章清单与章序建议

**结论先写:现有 21 章(不是派工书说的 20 章),行数合计 12,555;
章序建议「保序装订 + 加分部」,不要重排。** 不重排的理由不是保守:
成品章正文里有 **6 处写死的章号交叉引用**(见 §2),重排会让它们**全部指错**,
而项目里**没有任何关卡校验章号**——这正是 §2 那个重号能活到今天的原因。

生成命令与产出表:

```verify
cd /home/user/hermes-study && python3 data/r11c/f-pre-binding-inventory-scan.py 2>&1 | sed -n '1,2p'
```

```text
[1] chapters -> data/r11c/f-pre-binding-inventory-chapters.tsv  (21 章)
[2] overlap  -> data/r11c/f-pre-binding-inventory-overlap.tsv  (22 簇跨 >=2 章)
```

### 1.1 清单(按现行章号)

「章号」栏的依据见 §2:1–17 出自 `reports/round-9d-l1-completion.md` §8.2 那张表,
18–21 出自各轮报告与合并提交(= PR 描述)。

| 章号 | 文件(`chapters/`) | 轮次 | 行数 | 主题 | R12 建议位置与理由 |
|---|---|---|---|---|---|
| 1 | `r1-what-is-hermes-agent.md` | R1 | 208 | 这是什么:全仓地图 + 阅读顺序 | **第一部·进门**。它已自称「《设计蓝图》的第一章」,且唯一承担"怎么读这本书"的职责 |
| 2 | `r2-turn-loop-and-model-access.md` | R2 | 788 | 回合主循环与模型接入 | **第二部·内核** 开篇。一次 turn 的全过程是后面所有章的公共底座 |
| 3 | `r3-tool-infrastructure.md` | R3 | 412 | 工具系统:让 LLM 安全地动手 | 第二部。紧跟 turn 循环——工具调用是循环的另一半 |
| 4 | `r4-execution-environments.md` | R4 | 829 | 执行环境:一条命令在哪跑 | 第二部。是第 3 章「动手」的落地面 |
| 5 | `r5-session-state-and-persistence.md` | R5 | 730 | 会话状态与持久化 | 第二部。turn 之间靠它连起来 |
| 6 | `r6-memory-provider-ecosystem.md` | R6 | 545 | 记忆 provider 生态 | 第二部收尾。与第 5 章同主题的可插拔侧 |
| 7 | `r7-gateway-session-core.md` | R7 | 739 | 网关会话核心与多路复用 | **第三部·网关** 开篇 |
| 8 | `r7b-platform-integration.md` | R7B | 896 | 平台接入面(二十几个聊天软件) | 第三部 |
| 9 | `r7c-gateway-periphery-and-scheduling.md` | R7C | 571 | 网关外围面与定时调度 | 第三部收尾 |
| 10 | `r8a-configuration-surface.md` | R8A | 1771 | 配置面:一个键从哪来到哪去 | **第四部·配置与人机面** 开篇。全书最长的一章(占 14.1%),R12 应考虑拆卷或抽出速查附录 |
| 11 | `r8b-cli-trunk-and-interaction.md` | R8B | 563 | CLI 主干与交互 | 第四部 |
| 12 | `r8c-dashboard-and-web.md` | R8C | 428 | dashboard 与 web 面 | 第四部 |
| 13 | `r8d-self-custody.md` | R8D | 565 | 自持面:装在别人机器上怎么照顾自己 | 第四部收尾 |
| 14 | `r9a-capability-organization.md` | R9A | 547 | 能力的组织、扩展与委派 | **第五部·能力与对外** 开篇 |
| 15 | `r9b-multimodal-delivery.md` | R9B | 511 | 多模态交付面 | 第五部 |
| 16 | `r9c-external-interfaces.md` | R9C | 613 | 对外接驳:凭据、账单与信号 | 第五部 |
| 17 | `r9d-tool-surface-and-guard-placement.md` | R9D | 688 | 工具面:守卫装在哪一层 | 第五部收尾。**与第 3 章同主题、隔了 13 章**,见 §1.2 的取舍 |
| 18 | `r10-client-interface-layer.md` | R10 | 283 | 客户端接驳面:四种界面 | **第六部·客户端与交付** 开篇 |
| 19 | `r10b-desktop-application.md` | R10B | 308 | 桌面应用 | 第六部。`reports/round-10b-desktop-application.md:722` 自陈与第 18 章合起来才是完整界面层,R12 应把两章并为一部的相邻两节 |
| 20 | `r11a-ops-and-delivery.md` | R11A | 302 | 交付面:从源码到一台跑着的机器 | 第六部收尾 |
| 21 | `r11b-the-unwritten-layer.md` | R11B | 258 | 没人写的那一层:边角欠账 | **附卷**。它是横跨 R2/R4/R6/R7B/R8B 的补遗,不属于任何一部;见 §1.2 |

**「21 章 vs 派工书说的 20 章」**:第 21 章是 R11B 主线新增的,R11B 报告的口径注这样称呼它:

`reports/round-11b-review-and-reconciliation.md:207`

> *口径注:479 里有 5 条来自主线本轮新增的第二十一章;片 C 收工自测的自身贡献口径是 474。

派工书的「20 份」是漏计。这不是本片的判断误差,是清单本身的读数:

```verify
ls chapters/*.md | wc -l
```

```text
21
```

### 1.2 三条章序上的取舍(R12 要做决定,本片只摆事实)

1. **工具材料被切成三段,分别在第 3 / 14 / 17 章。**
   第 3 章讲工具系统的基础设施(注册、schema、执行),第 14 章讲能力怎么组织与委派,
   第 17 章讲守卫装在哪一层。三章之间隔了 13 章。
   *可选做法*:(a) 保序,在第 3 章末尾加「本主题的后两章在 14 / 17」的导读;
   (b) 把 14 / 17 移到 3 后面。**(b) 会打断第五部的内在顺序,并使 6 处章号交叉引用失效。**
   本片建议 (a)。
2. **第 10 章 1,771 行,是第二长章(896 行)的 2 倍、全书的 14.1%。**
   配置面天然是查阅型内容。建议 R12 把它的「配置项全表」部分下沉为附录,正文只留机制。
3. **第 21 章不是一个主题章,是一份补遗。** 它自陈「本章是它(第 8 章)的边角补遗」。
   装订成书时,一份按轮次产生的补遗放在正文末尾会让读者困惑「为什么现在才讲平台的边角」。
   *可选做法*:(a) 作为附卷「边角与欠账」保留;(b) 把它的各节并入对应主题章。
   **(b) 的代价是这一章现有的叙事(「没人写的那一层」)会消失**,而它恰好是全书唯一
   正面讲"harness 的边角是怎么欠下的"的地方。本片建议 (a)。

---

## 2. 章号核对:重号与跳号

**结论先写:有 1 处重号、7 个章号从未被任何地方声明过(只存在于一张表的序号列)。**
重号在**成品章正文里**,是 R12 会直接装订进去的那种错:

- **重号**:第 21 章正文把 `r7b-platform-integration.md` 称作**第十一章**——

  `chapters/r11b-the-unwritten-layer.md:255`

  > - 平台接驳的主干在第十一章 `chapters/r7b-platform-integration.md`;本章是它的边角补遗。

  而第十一章是 `r8b-cli-trunk-and-interaction.md`(§8.2 表 + 合并提交 `fa04d83`)。**r7b 是第八章。**
- **跳号(声明层面)**:3、4、5、6、8、9、10 这 7 个章号在全语料 + git 提交信息里
  **一次都没有被写出来过**,只存在于 §8.2 表的 `#` 列(见 §2.2 末尾三处引文)。
  实际章序**没有断档**(1–21 连续),所以这不是内容缺口,是**声明缺口**。

### 2.1 章号声明的全部落点

章号从来没有单一落点。本片扫了 `chapters/ reports/ notes/ reviews/ data/ scripts/` 的
`*.md` 与 `*.py`,**外加 `git log --all` 的提交信息(PR 描述的落点)**:

**两个读数(CLAUDE.md「搜过没有类测量」)**。**剔除**片 F 自身承载文件
(本底稿、`data/r11c/dispatch-brief.md`、`data/r11c/f-pre-binding-inventory-*`)后是 **25 处**,
这个数可重跑:

```verify
python3 data/r11c/f-pre-binding-inventory-scan.py >/dev/null 2>&1 && awk -F'\t' 'NR>1 && $4!="carrier"' data/r11c/f-pre-binding-inventory-numbering.tsv | wc -l
```

```text
25
```

**不剔除**的那个读数**原理上钉不住**:它把本底稿自己算进去,而本底稿每写一段讲章号的话
就会把它抬高一次。开工首测 **29 处**(承载贡献 4:派工书写了「第十二章、第十七章」,
探针注释写了两处「第 7 章」);写到本节完稿时已是 **121 处**(承载贡献 96)。
下面这块**不是可重跑命令的输出**,是收工时的一次性读数:

```text
[3] numbering-> ...(121 处「第 N 章」,其中片 F 承载文件贡献 96 处;剔除后 25 处)
```

**这正是 CLAUDE.md 说的「写一份点名清单就会改变下一次的读数」**,而且是它最干净的一个演示:
同一条命令、同一份语料,只因为**报告它的那份文件本身进了语料**,读数从 29 涨到 121,
而真正要看的那个数(25)一动没动。

25 处按落点分:

```verify
awk -F'\t' 'NR>1 && $4!="carrier"{c[$4]++} END{for(k in c) print k, c[k]}' data/r11c/f-pre-binding-inventory-numbering.tsv | sort
```

```text
chapters 7
gitlog 12
reports 5
reviews 1
```

**`chapters/` 那 7 处是本节的重点**:它们是会被装订进书里的章号,而 git 提交信息与报告里的
另外 18 处装订后就不再是正文了。7 处里 4 处指第 7 章、1 处自称第一章、1 处指第二章,
剩下**那一处就是重号**。

### 2.2 逐号对照表

「声明处」栏给的是**剔除承载文件后**的落点。

| 章号 | 应指(§8.2 + 报告/提交) | 声明处 | 判定 |
|---|---|---|---|
| 1 | r1 | `chapters/r1-what-is-hermes-agent.md:5`:`这是《设计蓝图》的第一章,`;`reports/round-3-tool-infrastructure.md:23`;`reviews/review-1-full-corpus.md:241` | 一致 |
| 2 | r2 | `chapters/r8d-self-custody.md:259`:`第二章讲的是"怎么调用一个模型"。这一节是**上游那半**:在调用之前,` | 一致(指代描述与 r2 标题「回合主循环与模型接入」相符) |
| 3–6 | r3 / r4 / r5 / r6 | **无** | **声明缺口** |
| 7 | r7 | `chapters/r7c-gateway-periphery-and-scheduling.md:110`、`:262`、`:389`、`:464` 共 4 处 | 一致 |
| 8 | r7b | **无**(且被 r11b 误称为第十一章,见下) | **声明缺口 + 被重号占用** |
| 9 | r7c | **无** | **声明缺口** |
| 10 | r8a | **无** | **声明缺口** |
| 11 | r8b | 合并提交 `fa04d83`「Merge round 8B: CLI 主干与交互面(第十一章 + 定案)(#12)」 | 一致 |
| 11 | **r7b(错)** | `chapters/r11b-the-unwritten-layer.md:255`:`- 平台接驳的主干在第十一章 \`chapters/r7b-platform-integration.md\`;本章是它的边角补遗。` | **▲ 重号** |
| 12 | r8c | 合并提交 `ed5558e`(标题与正文各一处) | 一致 |
| 13 | r8d | 提交 `dfe6935`;`reports/round-8d-cli-completion.md:133` | 一致 |
| 14 | r9a | 提交 `2eec955` | 一致 |
| 15 | r9b | 提交 `1888a1f` | 一致 |
| 16 | r9c | 提交 `7342577` | 一致 |
| 17 | r9d | 提交 `dfcccc7` | 一致 |
| 18 | r10 | `reports/round-10b-desktop-application.md:722` | 一致 |
| 19 | r10b | 提交 `719882e`;`reports/round-10b-desktop-application.md:722` | 一致 |
| 20 | r11a | 提交 `97ad57e` | 一致 |
| 21 | r11b | 提交 `a5b53b9`(标题 + 正文);`reports/round-11b-review-and-reconciliation.md:207` | 一致 |

权威表本身(17 章)在这里:

`reports/round-9d-l1-completion.md:347`

> ### 8.2 R12 待装订的成品章清单(17 章)

`reports/round-9d-l1-completion.md:358`

> | 8 | `chapters/r7b-platform-integration.md` | 平台接入 |

`reports/round-9d-l1-completion.md:361`

> | 11 | `chapters/r8b-cli-trunk-and-interaction.md` | CLI 主干与交互 |

**为什么这个重号能活下来**:章号不在任何关卡的检查面上。`verify_citations.py` 校验
`路径:行号`,`verify_evidence_commands.py` 校验 `verify` 块,两者都读不到「第十一章」这四个字。
r11b 那一行**同时**写了章号和文件名,而文件名是对的——所以任何以文件名为线索的检查都会放行。

### 2.3 章号交叉引用的排版不统一(R12 要归一)

7 处章内声明用了三种写法:`第一章` / `第 7 章`(阿拉伯数字 + 空格,4 处,全在 r7c)/ `第二章`、`第十一章`。
R12 装订时应统一,并且——**这是重点**——`第 7 章` 这种带空格的写法此前会被本片探针的
原始正则漏掉,而那 4 处正是唯一一处成品章互引章号的地方。修法与理由记在探针里:

`data/r11c/f-pre-binding-inventory-scan.py:63`

```
# 允许「第 7 章」这种带空格的写法。R11C 片 F 实测:不允许空格时,
# chapters/r7c-gateway-periphery-and-scheduling.md 里 4 处「第 7 章」全部漏掉,
# 而那正是唯一一处**成品章之间互相引用章号**的地方 —— 章号审计最该看的位置。
CHAPTER_NUM_RE = re.compile(r"第\s*([一二三四五六七八九十百零〇0-9]+)\s*章")
```

### 2.4 移交项 H-R11C-F-a

见 §5。

---

## 3. 跨章重复的条目

**结论先写:四条判据一起跑,得候选 22(A)+ 58(B)+ 0(C)+ 3(D)。**
A 的 22 簇经逐簇读正文后并成 **17 组判决**(有几组是同一件事被探针切成了两簇):
**8 组「R12 必须合并」**(§3.2,覆盖 10 簇)、**9 组「有意的交叉引用、不合并」**(§3.3,覆盖 10 簇)、
**2 簇「同文件邻近处的不同断言」**(§3.6)。D 的 3 簇另判(§3.6),B 的 58 条见 §3.4。
另有一条不是重复而是**冲突**:同一个覆盖率被两章用两种口径各报一次,谁都没标口径(§3.6)。

### 3.1 四条判据(判据要写出来,不凭印象)

| 判据 | 抓什么 | 为什么单列 | 候选数 |
|---|---|---|---|
| **A 同一处代码** | 同一基线源文件、行号相距 ≤30 行、被 ≥2 章引用 | 「同一条断言」的最强机械代理 | 22 簇 |
| **B 术语重复锚定** | 同一术语在 ≥2 章各自出现在「术语 + 解释标记」的句子里 | 硬标准 1(首次出现给解释)× 硬标准 6(每章独立可读)= **同一术语被 N 章各锚一次是制度的必然产物** | 58 个术语 |
| **C 事故重复讲述** | 同一 issue 编号出现在 ≥2 章的**正文**(围栏块外) | 硬标准 4 要求事故讲成故事;同一件事讲两遍就要合并 | **0** |
| **D 定案重复** | 同一基线**文件**上的 ▲/◇/■/◎ 出现在 ≥2 章 | 比 A 宽:一条定案的作用域常是整个文件而非某几行 | 3 |

重跑:

```verify
cd /home/user/hermes-study && python3 data/r11c/f-pre-binding-inventory-scan.py 2>&1 | sed -n '2p;6p'
```

```text
[2] overlap  -> data/r11c/f-pre-binding-inventory-overlap.tsv  (22 簇跨 >=2 章)
[6] dupes    -> data/r11c/f-pre-binding-inventory-dupes.tsv  (B 术语 58 / C 事故 0 / D 定案 3)
```

**判据 A 第一版是错的,数值 24 → 22。** 第一版没有跳过围栏块,于是**摘录块内部**的
`路径:行号` 也被当成本章的锚点。`chapters/r9c-external-interfaces.md` 有一段
` ```text ` 包着的 grep 输出,里面 10 行都长成「`./agent/file_safety.py` + 冒号 + 行号」的样子,
凭空造出 2 个跨章重叠簇(`agent/file_safety.py` 的 39–50 簇、`gateway/platforms/base.py` 的 1349–1370 簇)。
关卡本身早就跳围栏(下面这个 FENCE 分支),判据 A 也必须跳。
**两个读数分别标注:含围栏 24 簇、跳围栏 22 簇。不是同一个测量做了两遍。**

`scripts/verify_citations.py:666`

>         if FENCE.match(line):

### 3.2 判定为「R12 必须合并」的 8 组(覆盖 A 的 10 簇)

「保留在」栏给的是本片建议,理由都是**主题归属**:一段内容留在以它为主题的那一章。

| # | 簇 | 涉及章 | 各自怎么讲 | 建议 |
|---|---|---|---|---|
| M1 | `AGENTS.md:971-975` toolset 键清单 | 第 14 章 `r9a:429`、第 17 章 `r9d:643` | 14 章一行 ▲4 表格格(「列出 30 个键,其中 3 个不存在」);17 章有完整引文 + 58/30/漏 31/多列 3 的全套数,并自陈「结清 R9A 的一条移交项」 | **留 17 章**;14 章 ▲4 保留一行 + 指路。注意 14 章那一格的锚点是过期的,见 §4.2 |
| M2 | `agent/system_prompt.py:504-509` 技能索引放 volatile 层之首 | 第 5 章 `r5:469`、第 14 章 `r9a:233` | 两章都讲「索引会变,放 stable 会把缓存前缀从索引处炸到底」,14 章额外锚定了 prompt cache 这个术语 | **留 5 章**(提示词分层是它的主题);14 章缩为一句 + 指路,术语锚定上移到全局术语表(§3.4) |
| M3 | `gateway/run.py:2057-2060` 顶层标量 → 环境变量 | 第 10 章 `r8a:195`、第 12 章 `r8c:293`/`:299` | 10 章作为「桥③」完整讲;12 章为了铺 exfil 论证又引一遍 | **留 10 章**;12 章缩为一句 + 指路 |
| M4 | `hermes_cli/main.py:280` 配置读取函数第六个 | 第 10 章 `r8a:1742`、第 11 章 `r8b:552` | 10 章写成就地注记(「本轮清点为五个……R8B 复核后更正为六个」);11 章写成 ◇-R8B-b | **留 10 章**;11 章缩为指路。R12 要决定是否保留「本轮清点为五个」这句历史注记——它在单章里是诚实,在成书里是噪音 |
| M5 | `hermes_cli/web_server.py:1833-1840` 文件管理器读侧守卫 | 第 12 章 `r8c:245`、第 13 章 `r8d:378`/`:388` | 两章都引 `Read-side only: this guards list/read/download` 这句 docstring 论证写侧无守卫;13 章还多引了守卫本体 | **留 12 章**(dashboard 是它的主题);13 章缩为指路。**不能两处都留全文** |
| M6 | `tools/transcription_tools.py:336-350` + `tools/voice_mode.py:2193-2201` STT 名单三份、漂了一份 | 第 15 章 `r9b:405`/`:422`、第 16 章 `r9c:266`/`:289` | 同一个故事、同两段摘录、同一条 ■(少一个 `deepinfra`) | **留 15 章**(多模态交付面是它的主题);16 章缩为一句 + 指路 |
| M7 | `agent/file_safety.py:280-284` Bitwarden 缓存注释 + 1Password 同错未挡 | 第 16 章 `r9c:418`、第 17 章 `r9d:229` | 同一段注释、同一个「发生过一次、被记录、又犯一次」的故事;16 章更完整(带全仓 grep 与 1Password 那一半) | **留 16 章**;17 章缩为一句 + 指路 |
| M8 | `gateway/platforms/base.py:5711` + `gateway/run.py:11096` 忙时策略机先于 pending 槽 | 第 7 章 `r7:50`/`:58`、第 8 章 `r7b:778`/`:779` | 7 章给了两个代码块并注明「属平台接入面,下一轮细讲」;8 章用它证伪文档三句话中的一句 | **留 8 章**;7 章的两个代码块可删,一句话 + 指路即可(它自己已经这么声明了) |

### 3.3 判定为「有意的交叉引用,不合并」的 9 组(覆盖 A 的 10 簇)

这些位置**本来就写着指路**,或者是全景章刻意举例。R12 只需**把指路的写法归一**(见 §3.4):

| # | 簇 | 涉及章 | 为什么不合并 |
|---|---|---|---|
| K1 | `agent/agent_init.py:892` + `agent/conversation_loop.py:1415-1445`(`_budget_grace_call` 死代码) | 第 1 章 `r1:144`、第 2 章 `r2:141`/`:151`/`:157` | 第 1 章拿它当「文档-代码冲突」的示范例,第 2 章是机制正文。全景章举例是它的职责 |
| K2 | `agent/memory_manager.py:347-361` 记忆围栏 | 第 5 章 `r5:578`、第 6 章 `r6:292` | 第 6 章那一处**已经写着**「围栏全文见 R5 §3.7」,是正确写法 |
| K3 | `cli.py:479-481` `agent.personalities` 只在 CLI 默认值里 | 第 10 章 `r8a:397`、第 11 章 `r8b:169` | 10 章讲「键名校验只认 `DEFAULT_CONFIG`,于是假警告」,11 章讲「14 个内置人格」,是同一事实的两个后果 |
| K4 | 消息网关的人格命令列不出内置人格(`gateway/slash_commands.py:2502-2508`) | 第 10 章 `r8a:247`、第 11 章 `r8b:335` | 10 章是完整机制 + 实测;11 章那一行在「谁没跟上」三例表里,有独立叙事价值 |
| K5 | `tools/code_execution_tool.py:252-253` 沙箱环境洗净 | 第 3 章 `r3:278`、第 14 章 `r9a:186` | 3 章讲机制与 GHSA 修法;14 章用它证伪 `creating-skills.md` 的 never 那句(▲5) |
| K6 | `hermes_cli/web_server.py:12321-12346` pairing approve | 第 10 章 `r8a:1100`/`:1109`、第 12 章 `r8c:50`/`:56` | 12 章问「谁能调这条路由」,10 章讲「两把钥匙的分叉」 |
| K7 | `gateway/run.py:14479-14500` 配对码 | 第 7 章 `r7:689`、第 10 章 `r8a:1711` | 7 章是 ▲(文档把 `/pair` 方向写反),10 章是配置面的入站路径 |
| K8 | `cli.py:4488-4490` | 第 10 章 `r8a:426`、第 11 章 `r8b:218` | 10 章讲 28 个 CLI 专属键的假警告,11 章讲一层 `update` 让 14 个人格丢失 |
| K9 | `model_tools.py:1103-1123` | 第 3 章 `r3:54`、第 16 章 `r9c:365` | 3 章点名管线入口 `handle_function_call`,16 章讲 `post_tool_call` 钩子的载荷键 |

### 3.4 判据 B:58 个术语被 ≥2 章各锚定一次(R12 的全局术语表底稿)

这**不是谁写错了**——硬标准 1 要求每章首次出现的术语给解释,硬标准 6 要求每章独立可读,
两条加起来必然产生重复锚定。装订成一本书时它就变成 R12 要做的**术语表**。
全表在 `data/r11c/f-pre-binding-inventory-dupes.tsv`(`kind=B-term`)。本片按用途分三类:

- **业界通名(22 个,建议全局锚一次即可)**:`API`(11 章)、`CLI`(10)、`HTTP`(10)、
  `LLM`(9)、`JSON`(8)、`RPC`(5)、`URL`(5)、`ID`(4)、`SSH`(3)、`CI`(3)、`SDK`(3)、
  `ASCII`(3)、`AI`、`GPT`、`UI`、`POSIX`、`TLS`、`QQ`、`KEY`、`DELETE`、`README`、`terminal`。
- **项目内专名 / 需要一句话解释的技术名(建议进术语表并在首次出现处保留一句)**:
  `MCP`(7 章)、`SSRF`(3)、`PKCE`(3)、`TOCTOU`(2)、`ACP`(2)、`TUI`(3)、
  `SKILL.md`(2)、`config.yaml`(3)、`max_iterations`(2)、`execute_code`(2)、
  `read_file`(2)、`ANTHROPIC_TOKEN`(2)、`run_agent.py`(2)、`AGENTS`(2)、`LT`(2)。
- **误报(3 个)**:`None`、`except`、`status` —— 判据把「反引号里的英文标识符」一律当术语,
  这三个是代码里的普通词恰好被反引号括住。**点名留下,不修判据**:放宽会漏、收紧会误杀,
  而 3/58 的误报率对一份供人挑选的候选表是可接受的。

### 3.5 判据 A 的剩余 2 簇与判据 D 的 3 簇:全部判「不合并」

**这五条都逐条读过正文,没有一条按比例外推。**

| 簇 | 涉及章 | 各自断言 | 裁决 |
|---|---|---|---|
| A#6 `agent/skill_utils.py:849-877` | 第 14 章 `r9a:44`、第 20 章 `r11a:207` | 14 章讲 `SKILL_PROMPT_DESC_LIMIT = 60`(模型只看见一行索引);20 章讲 `iter_skill_index_files` | **不合并**:同文件两个不相干的符号,相距 28 行纯属探针窗口(30)内 |
| A#15 `hermes_cli/commands.py:92-106` | 第 9 章 `r7c:175`、第 10 章 `r8a:1014`/`:1512`/`:1539` | 9 章讲 `new` 子命令带 `aliases=("reset",)`;10 章讲 `VALID_BUSY_POLICIES` 这个名字承诺了它没有的强制力 | **不合并**:同一段 dataclass 定义的不同字段 |
| D#1 `gateway/run.py` | 第 8 章 `r7b:242`、第 10 章 `r8a:1304` | 8 章的 ▲1 讲「打断由第二层决定,文档说反了」(锚 `:23127`);10 章那格讲「顶层标量→环境变量的桥有两处实现」(锚 `:2058`) | **不合并**:判据 D 按**文件**归并,而 `gateway/run.py` 是全仓最大的文件之一,同文件相遇不构成同一条定案。这是 D 这条判据的已知代价 |
| D#2 `website/docs/developer-guide/gateway-internals.md` | 第 7 章 `r7:694`、第 8 章 `r7b:769` | 7 章讲 `:9` 的「20+ platforms」为什么记 ◎ 不记 ▲;8 章讲 `:86` 那句话里三件事有两件错 | **不合并**:同一份文档的两句话,而且两处正好示范了 ▲ 与 ◎ 的分界。R12 装订时可考虑放在相邻位置对照阅读 |
| D#3 `website/docs/user-guide/configuration.md` | 第 11 章 `r8b:552`、第 15 章 `r9b:485` | 11 章讲 `:1646` 的 `display.compact` 读取分支是死代码;15 章讲 `:1727` 的界面语言少列了 `ar` | **不合并**:同一份文档的两处不同过时 |

**判据 D 的诚实评价**:3 条候选全判不合并。它按文件归并、不看行号,所以在大文件与大文档上
必然产出这种「同文件不同断言」的候选。**不建议下一轮继续单跑 D**——它抓到的东西
判据 A 用更紧的窗口已经抓过了,而它自己的信噪比是 0/3。留在这里是为了让「跑过、结果为零」
这件事可查,而不是让下一轮再跑一遍。

### 3.6 一条不是重复而是冲突:同一覆盖率两个读数,都没标口径

`chapters/r8d-self-custody.md:328`

> 代价是这个保护**只在 4 个文件 5 处采用**,而全仓有 60 多个裸 `urlopen` 调用。◇

`chapters/r9c-external-interfaces.md:21`

> ——`hermes_cli/urllib_security.py`,设计得很完整(origin 归一化、头白名单、

`chapters/r9c-external-interfaces.md:22`

> 307/308 语义、防 addheaders 绕过)。全仓 25 个"自拼凭据头 + 标准库发出去"的调用点里,

同一个「`urllib_security` 被采纳了多少」,第 13 章报 **5 处 / 60 多**,第 16 章报 **2 个 / 25**。
两者**可以同时为真**(分母一个是裸 `urlopen` 调用、一个是自拼凭据头的调用点),
但**两章都没写自己的口径**。CLAUDE.md 定的规矩是「同一指标多次/多方法测量必须分别标注,
不得表述为读数相同」——装订成一本书之后,这两个数会在相隔三章的地方各说各的。
**R12 必须补口径,或统一到一个口径并注明另一个。**

---

## 4. 成品章里未同步的过期结论

**结论先写:确认 4 条**(1 条是整段数字全过期、2 条是定案被后续轮次改判而章内未动、
1 条是锚点被后续轮次更正而章内未动)。第 1 条最严重:它是**一段自己声明"这是当前值"的表**,
而现在跑一遍它指定的校验脚本会得到不一样的五行。

**独立性再声明**:本节从**成品章现状**出发反查(章侧锚点 / 章侧数字 / 章侧 ▲◇■◎),
未读片 E 的任何产出。本片收工时 `notes/r11c-raw-reversal-propagation.md` 不存在:

*(R11C 主线更正,只改取证方式、不改结论:原命令是 `ls notes/ | grep -c …`,量的是**当下
工作区** —— 片 E 后来到货,这条命令就从 0 变成 1,关卡当场判 EVIDENCE-DIFF。一条用来证明
「那时候还没有」的命令,不能钉在一个会长出那个东西的引用上。改钉片 F 入库的提交 `94be7f0`,
读数稳定,且语义正是本节要断言的那一句。)*

```verify
cd /home/user/hermes-study && git ls-tree -r --name-only 94be7f0 notes/ | grep -c 'r11c-raw-reversal-propagation' || true
```

```text
0
```

### 4.1 【最严重】第 1 章的分层表与进度数字整段过期,而它自称是当前值

`chapters/r1-what-is-hermes-agent.md:98`

> 五个层(`data/ledger.tsv` 台账,`scripts/verify_ledger.py` 校验)。**下表是当前值,不是历史快照**

现值与章内值对照(现值由下面这条命令算出):

```verify
cd /home/user/hermes-study && awk -F'\t' 'NR>1{sub(/\r$/,"",$4); n[$4]++; l[$4]+=$3} END{for(k in n) printf "%s\t%d\t%d\n",k,n[k],l[k]}' data/ledger.tsv | sort && awk -F'\t' 'NR>1{sub(/\r$/,"",$6); if($6=="R1-inventoried"){n++; l+=$3}} END{printf "R1-inventoried\t%d\t%d\n", n, l}' data/ledger.tsv
```

```text
L1	563	522207
L2	2131	671639
L3	1895	602085
L4	560	55902
LT	3381	756619
R1-inventoried	5944	1495470
```

| 章内位置 | 章内写的 | 台账现值 | 判定 |
|---|---|---|---|
| `chapters/r1-what-is-hermes-agent.md:103`:`| **L1 机制精读** | harness 核心机制,要逐行读透、能凭笔记重实现 | 511 | 479,923 |` | L1 = 511 / 479,923 | **563 / 522,207** | 过期 |
| `chapters/r1-what-is-hermes-agent.md:104`:`| **L2 结构级理解** | 支撑性代码,画得出结构、定位得到功能,不逐行 | 2,183 | 713,923 |` | L2 = 2,183 / 713,923 | **2,131 / 671,639** | 过期 |
| `chapters/r1-what-is-hermes-agent.md:111`:`> L1 的轨迹是 412(R1)→ 436(R6)→ 446(R7C)→ 461(R8A)→ **511**(R8B),L2 相应递减。` | 轨迹止于 511(R8B) | 其后 R8D/R9A/R9B/R9C/R9D 各有增量,现为 563 | 过期 |
| `chapters/r1-what-is-hermes-agent.md:118`:`**不**回答"它学过没有"。后者在台账的 `status` 列里,当前仍有 **8,122 个文件 / 2,236,870 行**` | 8,122 文件 / 2,236,870 行 停在 `R1-inventoried` | **5,944 / 1,495,470** | 过期 |
| `chapters/r1-what-is-hermes-agent.md:119`:`停在 `R1-inventoried`(= 只盘点过、没开工),即 8,530 − 8,122 = **408 个文件**被真正处理过。` | 408 个文件被处理过 | 8,530 − 5,944 = **2,586** | 过期 |
| `chapters/r1-what-is-hermes-agent.md:123`:`一个反直觉的数字:**测试(756,619 行)比核心机制(479,923 行)还多**。这不是坏事——它意味着几乎每个` | 核心机制 479,923 行 | **522,207**(结论方向仍成立) | 过期(数)/ 成立(论) |

**这条为什么值得排在第一**:同一处**在 R8-fix 已经被修过一次**。
`reports/round-8-fix-review-1.md` 的 M-2 判「阻断 / 采纳」,把这张表从 R1 期的手抄件改成台账当时值,
并加了「这张表会动」的演进说明。章内 `:112-116` 那段自陈把教训写得很清楚:

`chapters/r1-what-is-hermes-agent.md:113`

> 早先本章印的是 R1 期那一版(412 / 382,770 与 2,282 / 811,076),**却以现在时陈述**——

修法当时选的是「把手抄件换成新的手抄件 + 说明它会动」,而不是「不要手抄」。
**于是同一个形态在 6 轮后原样复发。** 章内 `:116` 自己写下的教训正是这一条:

`chapters/r1-what-is-hermes-agent.md:116`

> 正文就不该有第二份手抄件;抄了就得说清抄的是哪一天。

**给 R12 的执行建议**:要么把这张表改成「R12 定稿日快照 + 日期」,
要么删表只留校验命令。**不要再抄一次当前值**——装订之后台账还会动。

### 4.2 第 14 章 ▲4 的锚点已被 R9D 更正,章内未同步

`reports/round-9d-l1-completion.md:187`

> 另**更正 R9A 的锚点** `AGENTS.md:971-974` → `971-975`:原范围**漏掉整整一行 8 个键**,

`chapters/r9a-capability-organization.md:429`

> | ▲4 | `AGENTS.md:971-974` | 列出 30 个 toolset 键 | 其中 `messaging` / `moa` / `rl` **不存在** |

基线那份清单占 `AGENTS.md` 的 971–975 共 5 行,按 971–974 截取只数得出 22 个键,
而这一格声称的是 30 个——**锚点与它自己的断言对不上**。

`AGENTS.md:975 @ 863e313`

```
`spotify`, `terminal`, `todo`, `tts`, `video`, `vision`, `web`, `yuanbao`.
```

**执行**:第 14 章 `:429` 那一格的锚点范围由 971–974 改为 971–975。断言本身(3 个键不存在)不变——
R9D 的定案确认 3 个 ▲ 成立,被推翻的是**另外 31 个**该记 ◇ 还是 ▲,而第 14 章从没写过那 31 个。

### 4.3 第 13 章把一处 ◇ 记轻了,R9D 已定案为 ■

`chapters/r8d-self-custody.md:328`

> 代价是这个保护**只在 4 个文件 5 处采用**,而全仓有 60 多个裸 `urlopen` 调用。◇

`notes/r9d-90-handover-credential-landing.md:947`

> | **H-R9C-a** | **改判**:env 分支非缺陷(推翻);stored 分支降级为设计缺口;**新增 ■ = 裸 `urlopen` 跨源重定向泄漏 Bearer**,与 H-R9A-a 同型 | `hermes_cli/nous_billing.py:413`:`with urllib.request.urlopen(req, timeout=timeout) as resp:`;对照 `hermes_cli/urllib_security.py:112` 的 `open_credentialed_url`;策略依据 `hermes_cli/auth.py:2340`:`gated by ``_NOUS_PORTAL_ALLOWED_HOSTS``: that allowlist exists to reject` | 跑 `redirect_probe.py`(见 1.5),看 B 端是否收到 `Authorization`;再跑上面那 6 个测试文件确认 49/0 |

第 13 章记的是 **◇**(「代码有、文档无」),落点是「采用率低」。R9D 用一个跨源 302 探针实证了
**未采用处会把 `Authorization` 带过去**,并**新立 ■**。第 13 章的 §5 合计行
因此也是按旧定案统计的:

`chapters/r8d-self-custody.md:539`

> 本簇合计 **▲ 12 / ◇ 8 / ■ 10 / ◎ 1**(逐条证据见底稿)。三条最值得记的:


**执行**:第 13 章 `:328` 由 ◇ 升为 ◇ + ■(或改述为「◇ 采用率低 → 见第 17 章 ■」),
并同步 `:539` 那一行的合计;R12 若统一重算跨章 ▲/◇/■/◎ 总数,这一条要计进去。
*注意口径:第 13 章那行合计是「本簇」口径,不是全书口径;改它会改跨轮可比性,
建议 R12 以脚注形式补 ■ 而不是直接改合计数。*

### 4.4 第 13 章与第 16 章的覆盖率两个读数

同 §3.6,不重复。它既是「跨章重复/冲突」也是「未同步」——因为第 16 章(R9C)的读数**更晚**,
而第 13 章(R8D)那句从没被回看过。

### 4.5 判过、确认**没有**过期的(负结论要给搜索面)

以下几条是本片查过、确认成品章**已经同步**的,写出来是为了让下一轮不必重查:

| 被后续轮次改判的结论 | 成品章现状 | 判定 |
|---|---|---|
| `config.yaml` 读取函数 五个 → 六个(R8B) | `chapters/r8a-configuration-surface.md:18`:`3. 但**同一份 `config.yaml` 有六个读取函数**(本轮清点为五个,R8B 补上了启动最早期的` | 已同步 |
| r7b 交互方法「三个没有基类桩」→「五个里两个有桩」(R8-fix M-4a) | `chapters/r7b-platform-integration.md:819`:`**这五个交互方法里,两个有基类桩(`send_clarify`、`send_slash_confirm`)、三个没有**` | 已同步 |
| 「看板评论 steer」定位错(R7C 更正 R7) | 第 7 章正文已无该说法;第 9 章 `chapters/r7c-gateway-periphery-and-scheduling.md:464`:`顺带更正上一轮的一处定位:第 7 章曾把"看板评论 steer"归到网关的看板 watcher 文件里。` 保留了更正记录 | 已同步(**但 R12 要决定这段过程性文字留不留**) |
| 漏列 31 个 toolset 整体判 ▲ → 24 ◇ + 7 ▲ + 3 ▲(R9D 推翻主线初判) | 第 17 章 `chapters/r9d-tool-surface-and-guard-placement.md:667`:`(本章初稿把 31 个整体算作 ▲,是错的;更正依据是上面那次定稿快照对读。)` | 已同步 |
| `approvals.deny` 落盘截断「静默抹掉」→ R8C 推翻(有 stderr + 逐字备份) | 全 `chapters/` 搜「静默抹掉」「静默地抹」零命中(搜索面:`grep -rn` 全部 21 个 `chapters/*.md`,不排除任何段落,含围栏块) | 未进成品章 |

搜索面申报:

```verify
cd /home/user/hermes-study && grep -rc "静默抹掉\|静默地抹" chapters/ | grep -v ':0' | wc -l
```

```text
0
```

### 4.6 机械候选与人工裁决的差(覆盖面要如实说)

本节的 4 条不是从一条判据里掉出来的。三条机械判据各自的产量与噪声:

| 判据 | 机械候选 | 经人工读正文后确认 | 说明 |
|---|---|---|---|
| 甲·点名法(后轮语料点名某章 + 上下文带改判语) | 4 | **0** | 4 条全是**排版/关卡类**的点名(UNCHECKED 占比、表格锚点),不是结论改判 |
| 乙·锚点法(后轮在同一基线位置 ±30 行 + 改判语) | 67 | 0(未新增) | 噪声主导:「改判语」常出现在与该锚点无关的邻句 |
| 乙′·限定在**章侧 ▲◇■◎ 行**的锚点 | 10 对 / 5 处 | **1**(§4.2) | 收窄后信噪比大幅提升,是三条里唯一直接命中的 |
| 数字法(章内「N 个」vs 后轮同符号不同数,且后轮行带改判语) | 621 → 14 | **2**(§4.3 的 urlopen、§4.5 已同步的两条) | 621 是未过滤量,加「后轮行必须带改判语」后剩 14 |
| 台账对读(章内手抄的台账数 vs 现台账) | — | **1**(§4.1) | **不是上面任何一条判据抓到的**,是顺着「哪些数字会自己变」想出来的 |

**如实说**:§4.1 那条——本节最严重的一条——**没有任何机械判据抓到它**。
它的形态是「章内数字与**本仓库另一份可计算数据**不一致」,而三条判据全都是
「章内说法与**后轮语料的说法**不一致」。这类「与可计算事实对表」的检查目前是空白;
移交为 `H-R11C-F-b`。

---

## 5. 移交

本片是「只报不改」,所以三份清单本身**全部是给 R12 的执行项**,不重复列进移交表;
这里只列**需要下一轮/主线决策或补机制**的四条。锚点按 R9B 规矩写成声明式(锚点后紧跟反引号摘录)。

| 案号 | 现象(一句话) | 锚点 + 摘录 | 建议去向 |
|---|---|---|---|
| **H-R11C-F-a** | 成品章正文里的章号重号:第 21 章把 r7b 叫作第十一章,而第十一章是 r8b(§8.2 表 + 合并提交 `fa04d83`)。**章号不在任何关卡的检查面上**,所以它能一直活着 | `chapters/r11b-the-unwritten-layer.md:255`:`- 平台接驳的主干在第十一章 ` | R12 装订时改正;若 R12 决定给章号加一道校验,判据是「章号 → 文件」映射与 §8.2 表 + 各轮提交的声明一致 |
| **H-R11C-F-b** | 「章内手抄的数字 vs 本仓库可计算数据」这类过期**没有任何判据在查**。本片最严重的一条(§4.1)不是被三条机械判据抓到的,是顺着「哪些数字会自己变」想出来的 | `chapters/r1-what-is-hermes-agent.md:98`:`五个层(`data/ledger.tsv` 台账,`scripts/verify_ledger.py` 校验)。**下表是当前值,不是历史快照**` | 建议做成一条关卡:凡正文里出现台账/分层/进度数,与 `data/ledger.tsv` 现值对表。**同一处已在 R8-fix 修过一次又复发**,靠人记得回看是不够的 |
| **H-R11C-F-c** | 第 13 章把 `urllib_security` 的覆盖缺口记 ◇,而 R9D 已就同一处**新立 ■**(跨源 302 实测泄漏 Bearer);第 13 章 §5 的合计行因此也是按旧定案统计的 | `chapters/r8d-self-custody.md:328`:`代价是这个保护**只在 4 个文件 5 处采用**,而全仓有 60 多个裸 ` | R12 装订时升级标记;**不要直接改 §5 合计数**(它是「本簇」口径、跨轮可比),建议加脚注 |
| **H-R11C-F-d** | 同一个「`urllib_security` 采纳率」被两章用两种口径各报一次(第 13 章 5 处/60 多;第 16 章 2 个/25),**两章都没写自己的口径** | `chapters/r9c-external-interfaces.md:22`:`307/308 语义、防 addheaders 绕过)。全仓 25 个"自拼凭据头 + 标准库发出去"的调用点里,` | R12 补口径或统一;这是 CLAUDE.md「同一指标多次/多方法测量必须分别标注」在**跨章**场景下的第一例 |

**没有铸新号的两件事,说明为什么不铸**:

- §3.2 的 8 条合并项与 §3.4 的术语表:它们是**装订动作**,不是未决调查。铸成移交号会让
  R12 的执行清单和移交账本各存一份,而 CLAUDE.md 刚定过「结清的单一落点」——
  一份清单两个存放地正是它要防的形态。
- §2 的「7 个章号从未被声明」:实际章序无断档,声明缺口在装订后自动消失(书里每章都有号)。
  它是本片的观察,不是欠账。

---

## 完成信号

**片 F 完成。** 产出:

- `notes/r11c-raw-pre-binding-inventory.md`(本文件)—— 三份清单 + 移交 4 条。
- `data/r11c/f-pre-binding-inventory-scan.py` —— 唯一探针,六段判据全写死在里面,重跑即复现。
  本轮对它做了四处修正:章号正则允许「第 7 章」的空格写法;章号扫描加 `git log --all`
  (PR 描述是章号的主要落点,原版扫不到);判据 A 改为跳过围栏块(原版把摘录块里的
  `路径:行号` 当成本章锚点,虚增 2 簇);新增判据 B/C/D。
- `data/r11c/f-pre-binding-inventory-chapters.tsv` —— 21 章清单(行数/结构计数/锚点数/标题)。
- `data/r11c/f-pre-binding-inventory-overlap.tsv` —— 判据 A 的 22 簇候选(含章内行号)。
- `data/r11c/f-pre-binding-inventory-numbering.tsv` —— 121 处「第 N 章」声明(含 `corpus` 列,
  可按 `carrier` 剔除本片承载文件)。
- `data/r11c/f-pre-binding-inventory-dupes.tsv` —— 判据 B/C/D 的候选(58 / 0 / 3)。
- `data/r11c/f-pre-binding-inventory-staleness.txt` —— 判据甲/乙的原始命中(67 处,含上下文)。
- `data/r11c/f-pre-binding-inventory-numeric.tsv` —— 数字法的 621 对原始候选。

**未改动任何 `chapters/`、`reports/`、`notes/`(本文件除外)、`scripts/` 或基线文件。**
未 commit、未 push。

**关卡自查(收工读数)**

```text
python3 scripts/verify_citations.py /home/user/hermes-agent notes/r11c-raw-pre-binding-inventory.md
  citations=21  OK=17  UNCHECKED=4
  可校验比例 OK/21 = 81.0%              (下限 70%,底稿口径 —— 通过)
  table_anchors=42  OK=12  UNCHECKED=30 (0 DRIFT / 0 OUT-OF-RANGE)
  OK: every code-block-backed citation matches the baseline        退出码 0

python3 scripts/verify_evidence_commands.py notes/r11c-raw-pre-binding-inventory.md
  verify-blocks paired=8  unpaired=0  differing=0  timedout=0
  runnability   ran=0  runfail=0  skipped-mutating=0
  OK: every paired ```verify command reproduces its pasted output  退出码 0

git -C /home/user/hermes-agent status --porcelain
  (空)
```

*自校验读数贴在 ```` ```text ```` 里而不是 ```` ```verify ```` 里:关卡会重跑 verify 块,
而一条「跑关卡扫本文件」的命令会无限递归(CLAUDE.md 已定,R11B 片 A 实测挂住过)。*
