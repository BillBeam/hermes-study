# Round 8B 报告 · CLI 主干与交互

**一句话结论:主干学透,两笔旧账算清,同一形状抓到七例。**

头条不是某个 bug,而是一个在本轮反复出现、且**上一轮已经撞到过一次**的形状:
**两份必须一致的东西,中间只有人。**

它有两副面孔。第一副是**改了但没传到全部消费面**。最干净的样本是一个
**从来没有显示过的启动提示**:Hermes 本该在启动时告诉你"某些工具因为缺 API key 被禁用了",
数据生产方给的字段叫 `env_vars`(`tools/registry.py:904 @ 863e313`),
而消费方用下标取的是改名前的 `missing_vars`(`cli.py:7565 @ 863e313`)。
抛出的 `KeyError` 被一个注释写着"别因为 import 出错而崩溃"、
作用域却覆盖整个函数体的 `except` 吞掉(`cli.py:7576 @ 863e313`)。
**最恶劣的一点在最后:这个异常只在"有东西要显示"时才抛**
——没有工具被禁用时列表为空、循环不进去、不抛异常。
**于是"功能坏了"和"一切正常"在屏幕上完全一样。**
而同一仓库的 `hermes doctor` **跟上了这次改名**,用的是
`item.get("missing_vars") or item.get("env_vars")`(`hermes_cli/doctor.py:2548 @ 863e313`)。
**一个消费面跟上了,另一个没有,而落后的那个是最安静的那个。**

第二副面孔是**两份必须手工对齐的名单**。本轮撞到四份,其中"斜杠命令注册表 vs `if/elif` 分发链"
已经付出代价:`/whoami` 在注册表里登记且不带任何 surface 限定
(`hermes_cli/commands.py:181 @ 863e313`),于是 `/help` 会列出它;
而 `cli.py` 全文**没有任何一处**提到 `whoami`——敲下去得到 "Unknown command"。
**最值得记的是仓库自己的记录**:根 `AGENTS.md:385` 声称所有消费方从注册表**自动派生**,
`AGENTS.md:402` 又要求手写一条 `elif`——**同一份文档的两句话互相矛盾,而 `/whoami` 是它的现价**。
更说明问题的是:同类问题此前已修过两次(`/indicator`、`/sessions`),
**两次都只加了针对那一条命令的回归测试,没有加"遍历注册表逐条确认分发链认识它"那条结构性断言**,
于是第三条照样漏出来。**只修实例不修判据,下一个实例一定还会来。**

**而这个形状的第三副面孔最狠:两处代码完全一致,含义却不同。**
`cli.py` 拆巨文件时把一批处理器**逐字搬进** mixin,**恰恰是"一字未改"让它坏了**——
`global _active_agent_ref` 在 `cli.py` 里绑的是 `cli` 的全局,搬进 mixin 后绑的是
**mixin 自己的**全局,于是清理逻辑读 `cli._active_agent_ref` **永远读到 `None`**,
`/exit` 时**记忆提供方的会话结束钩子从来没被触发过**(`hermes_cli/cli_agent_setup_mixin.py:520 @ 863e313`
的注释一字不差地记着这件事,并给了 issue 号 #49287)。
**教训很锋利:`global` 的含义取决于这段代码住在哪个模块,
所以它是搬家式重构里最不安全的一类语句——移动它不报错、不告警、类型检查也看不见,
只会安静地开始写另一个变量。**

**上一轮欠的两笔账(H-1 / H-2)本轮算清,而且在同一个键上合流了。**
H-2 问"浅合并的实际影响面有多大",答案比预期小得多也精确得多:
一层 `update` 只会让**三层深**的键丢兄弟,`cli.py` 的默认值里这样的子树**恰好 4 棵、共 24 个叶子**
——这是完整穷举,不是抽样。其中 **10 个被读取点的兜底救回来了**(兜底值恰好等于默认值),
**14 个没有**。没救回来的正是 `agent.personalities` 的 14 个内置人格,因为它们的兜底是 `{}`。
**实跑复现:用户在配置里加一个自己的人格,14 个内置人格全部消失**(对照组无配置时为 14)。

由此得到一条比"别写两份默认值"更可用的判据:
**浅合并的危害不取决于合并本身,取决于读取点的兜底能不能重建被删掉的东西。**
`host.docker.internal` 那种**标量**兜得回;`{}` 兜不回**十四条内容**。
**默认值装的是"内容"而不是"取值"时,浅合并一定丢内容。**

而 H-1 顺着同一个键走出了第三种后果形态。上一轮的两种是"值不同"(clarify 漂 780 秒)与"假警告";
**这一种是键的层级不同**:`agent.personalities` 只存在于 `cli.py` 那份默认值里,
主装载器的同名概念挂在**顶层**且为空(`hermes_cli/config_defaults.py:2126 @ 863e313`),
而**全部读取点读的都是 `agent.personalities`**。四个消费面实跑对照(全新安装、无 `config.yaml`):

```
1) CLI 面      cli.py:4490                  -> 14 个
2) 主装载器    load_config()                -> 0 个
3) 消息网关面  _load_gateway_config()       -> 0 个
4) TUI 网关面  server.py:5811               -> 14 个
```

**同一条 `/personality` 命令,电脑上列出 14 个内置人格,手机上通过 Slack 列出 0 个。**
连带发现顶层那个 `personalities` **没有任何读取点**(死键),
**而它的注释"add your own entries here"恰好把用户往错的层级引**,
且校验器为这个错层级专门开了绿灯(`_OPEN_DICT_TOP_LEVEL_KEYS`,`hermes_cli/config.py:4661 @ 863e313`)
——**用户写错层级不会收到任何警告**。

**把三种死法摆在一起,是本轮认为最有教学价值的一张表**,因为三者的报错信息都是"没有报错",
但排查手感完全不同:**值漂移**(读到了,值不对)、**层级错位**(某些面读不到)、
**读它的代码根本跑不到**(`display.compact` 的 `compact is not None` 恒为真,
因为形参标注是 `bool = False`——配置分支是死代码,而文档还在教用户设它)。

---

## 0. 执行说明

- **实际执行模型**:运行时策略明确禁止把模型标识写入推送到仓库的产物(commit message /
  PR / 代码注释 / 任何仓库文件),因此**模型标识在本会话最后一条回复中给出,不写入本报告**。
  可写入的依据部分如实记录:标识来自运行环境系统提示的显式声明,**无独立第三方自证渠道**。
  这是本轮唯一一处对任务书字面要求的偏离(与 R7B / R7C / R8A 同一处置)。
- **子代理探针(R7C 立、R8A 沿用,本轮开轮第一个动作)**:发一个只读单文件探针
  (Read + Grep + Bash 各一次,目标 `hermes_cli/cli_commands_mixin.py`)。
  **三个工具调用全部成功**,返回 sha1 `f0b93d9a…`、行数 3556、58 个方法名与行号,
  与主线复核一致。**结论:子代理完全可用。**
  探针**同时回答了 R8A 要求下一轮开轮必核的两个约束之一**:`nproc` = **4**
  ⇒ 并发上限 2,与 R8A 实测一致。
- **本轮实际执行方式**:采纳 R8A §11 的建议,**把段数从 14 压到 8**(每段更粗),
  且**不走 workflow,改为主线直接派发独立子代理**、分三批(3 / 3 / 2)。
  主线**自己承担了入口链那一段的正面精读**(`notes/r8b-01`),不外包。
  **本轮未触发会话级用量上限**——R8A 那次是 14 段跑到第 8 批时被打断,
  本轮 8 段全部跑完。**段数压缩这条建议有效,予以确认。**
- **主线复核制度(R8A 立)照常执行**:子代理报的高价值条目**不直接采信**,
  主线逐条回源验证。本轮主线独立复核并确认的有:`display.compact` 死配置、
  `missing_vars` / `env_vars` 改名漏跟(含 `doctor.py` 的对照)、`/whoami` 有注册无分发、
  `/undo` 非法参数导致退出、审批裁决 fail-open。**详见 §4。**
- **一条方法学观察(本轮新得)**:**任务书里把前提写成"可被否定的断言"这条,收益比 R8A 报的还大。**
  本轮给 8 个子代理各写了 3 条前提,**回报里明确"前提为假"的占压倒多数**——
  例如"`HermesCLI.__init__` 是个普通构造函数"被证伪:它 541 行,
  开 SQLite 并建表、跑两套磁盘维护、读 JSON、改模块全局、**并可能发一次 HTTP 请求**;
  "`_recover_after_resize` 是因为 prompt_toolkit 自己渲染坏了"被证伪为**因果颠倒**;
  "状态栏每次按键重建"被证伪为**那个函数根本不在热路径上**(只在 except 兜底里出现)。
  **写错的前提比写对的前提更有价值**,因为它逼出了解释;建议此后固定这么写。

---

## 1. 范围调整(任务书允许,理由如下)

开轮台账 `round=R8B` 为 **48 文件 / 40,804 行**。本轮**增补 2 个文件、未删任何文件**,
调整后 **50 文件 / 43,539 行**。两条都以规则写进 `scripts/assign_layers.py`(非手改台账)。

| 增补 | 行数 | 理由 |
|---|---|---|
| `hermes_cli/_parser.py` | 473 | R8B 的范围描述原文就是"进程入口、**argparse 子命令树**、命令分发 mixin",而顶层 parser 与 `chat` 子 parser 的本体全在它里面。落进 R8D 只因规则表没点名、被 `hermes_cli/**` 兜底吃掉——**是规则表的漏,不是判断的改** |
| `hermes_cli/profiles.py` | 2,262 | **R8A 报告 §1 明确写了"留 R8B"**,规则表同样漏了点名;且 `--profile/-p` 在 argparse **之前**就被消费,与主干耦合到无法分开读 |

**R8B +2,735 行,R8D −2,735 行,逐位平衡。** 重生成后 `path/kind/lines/status`
四列与切前**逐字节相同**(`diff` 实测),仅 `layer` / `round` 两列变化。

**没有增补的相邻文件**(读到但不认领,沿用 R8A 先例):`tools/approval.py`(R3 已精读)、
`hermes_cli/status.py` / `config*.py` / `commands.py`(R8A 已精读)、`tui_gateway/server.py`(R9/R10)。

---

## 2. 分层修订:50 个文件 L2 → L1

促升理由同 R6 / R7C / R8A 先例:**轮次主题本体留在 L2,与「本轮达成 L1 完成标准」直接冲突**。
规则落在 `assign_layers.py`,重生成 diff 恰好只有分层与轮次两列。

---

## 3. 主要学习成果(成品章 `chapters/r8b-cli-trunk-and-interaction.md`)

成品章按 CLAUDE.md 的六条可读性标准写成,面向"没读过本仓库、不熟 Python 异步生态"的后端工程师。
骨架:一次 `hermes -p work chat` 的走法 → 全景图 → 配置键的三种死法 →
"两份必须一致的东西"的两副面孔 → 主干上另外两个值得学的设计 → 可迁移原则 → 地图出入。

除头条外,本轮认为最值得带走的三条:

**(a) 入口顺序是一种资源,而且它会产生债务。** `-p` 决定 `HERMES_HOME`,
`HERMES_HOME` 决定读哪份配置,而很多模块在 import 那一刻就读配置
——所以 `-p` **必须在 argparse 之前**被手工扫出来(`hermes_cli/main.py:517 @ 863e313`)。
代价是一份**手抄的"带值参数"名单**(`hermes_cli/main.py:572 @ 863e313`)与真 parser 之间**没有任何自动校验**。
这笔债做得很细致(`mcp add --args` 透传区不扫描、pytest 的 `-p no:xdist` 被正则挡掉、
`sudo` 下按 `SUDO_USER` 找 profile 库),**但它仍然是本轮"手工对齐名单"四份里的第一份**。

**(b) 修复命令必须能在"被自己搞坏的环境"里跑起来。** 入口第一个 import 被 try 包着,
理由写得极具体:`hermes update` 若在 `git reset --hard` 与 `pip install -e .` 之间崩溃,
会留下"新源码 + 旧 `.pth`"的组合;**没有这个 guard,用户连修复命令都敲不进去**
(`hermes_cli/main.py:50 @ 863e313`)。同处还有一条判据很漂亮:自愈被打断的安装时
故意用**不精确**的匹配,因为"两种误判的代价不对称,选择往代价小的一侧犯错"
(`hermes_cli/main.py:11215 @ 863e313`)——**而且把这个不对称写进了注释**,
否则后人会把它"修"成精确匹配。

**(c) 当"正常完成"和"失败"用同一个异常类型表达时,必须找到区分二者的字段。**
主干为绕开旧版 Python 的子命令路由问题会**条件性地解析两次**,第一次把 stderr 换成内存缓冲吞掉。
第一版补丁让 `hermes model --help` **把帮助打了两遍**——因为 `--help` 也抛 `SystemExit`。
修法是按**退出码**区分(`hermes_cli/main.py:12507 @ 863e313`)。

---

## 4. 定案

### 4.1 主线独立复核并确认的条目(不采信、逐条回源)

| 编号 | 结论 | 关键证据 |
|---|---|---|
| ■-R8B-01 | `agent.personalities` 是 CLI 专属键,四个消费面两个看不见内置人格;仓库已在两处打补丁并留注释,**消息网关漏了** | 四面实跑对照;`hermes_cli/commands.py:2022` 的注释一字不差地写出病根 |
| ■-R8B-02 | 浅合并受害面 = 4 子树 / 24 叶,**14 个真出事** | AST 穷举 + 实跑 14→1,含无配置对照组 |
| ■-R8B-03 | `display.compact` 是**死配置**:`compact is not None` 恒为真 | `cli.py:4223` 标注 `bool = False`;全仓仅两处生产构造,均传真 bool |
| ■-R8B-04 | 启动"工具被禁用"提示**从未显示过**,且与"一切正常"不可区分 | 生产方 `env_vars` vs 消费方 `missing_vars`;`doctor.py:2548` 是跟上了的对照 |
| ■-R8B-05 | `/whoami` **有注册、无分发**,`/help` 列出但敲不出来 | `commands.py:181` 无 surface 限定;`cli.py` 全文零匹配 |
| ■-R8B-06 | `/undo` 非法参数**直接退出 CLI 会话** | `cli.py` 中 `process_command` 声明 `-> bool` 却有裸 `return`;调用点 `if not …: self._should_exit = True; app.exit()` |
| ■-R8B-07 | 审批裁决 **fail-open**:非 `timeout`/`deny` 一律视为批准 | `tools/approval.py` 尾部 `return {"approved": True, ...}`;**当前无活体 bug**,因 cli.py 的输出字母表恰好封闭 |

### 4.2 负结论(同样是产出)

- **H-7 关闭**:没有第三个"读原始配置后落盘"的截断路径。13 行坏 YAML 实跑,
  文件 **sha1 与字节数逐位未变**。但**保护是副作用不是设计**:坏 YAML 下
  `check_config_version()` 返回 `(33, 33)`(当前==最新),迁移阶梯整个没跑。
  连带可观察后果:**配置坏掉时版本检查会报"已是最新"**。
  **公平记一条**:同一次运行 stderr 打了很明确的警告并自动存了 `.corrupt.<时间戳>.bak`,
  故只作信息记录(◇-R8B-a),不升为缺陷。
- **H-16 关闭,而且它是本仓库做对的样本**:v16 不删旧键**是有意的**,
  与 R8A ■-11 那条 780 秒漂移是**同一道题、相反结果**,差别有三处不是一处
  (旧键不进默认值 / 规范键优先 / 迁移不覆盖已有新值)。
  **由此把 R8A 的教训补全为更好用的判据:先保证"存在 == 用户设过",再让规范键赢。**
- **▲-R8B-01**:两份 TUI 判定对 `HERMES_TUI=1` 的语义分类**相反**(explicit vs ambient),
  逐个核完三个消费点后**当前无后果**——每个消费点都独立再防了一次。
  **记它的理由不是它会出事,而是它不出事的原因是纵深防御、不是优先级正确**;
  任何一处防护被当作冗余删掉,那段 docstring 里描述过的 kanban 事故就会以新形态回来。
- **◇-R8B-b**:`config.yaml` 读取函数应从 R8A 的**五个**更正为**六个**;
  第六个是 `_config_default_interface_early`(`hermes_cli/main.py:280`),
  **且它是唯一一个有正当理由的重复**——它的调用点在模块级第 371 行,
  而复用共享缓存的那个桥在第 709 行才 import 配置子系统,**371 < 709**,此时确实无缓存可用。

### 4.3 主线收窄的一条

**H-17**:锁不对称**属实**(三条改写 `_APPLIED_HOMES` 的路径只有一条持锁),
但子代理转述的"**缓存被空 dict 覆盖**"**不成立**——它是 `set`、用 `.clear()` 而非赋值,
且 CPython 下这些操作是 GIL 原子的,不会撕裂出损坏容器。
**真正成立的是**:(a) check-then-act 竞态导致**重复的外部密钥拉取**
(窗口宽度 = 一次 vault 网络往返),(b) `reset_secret_source_cache()` 的三个容器**非原子清理**。
**代码确证,运行时未复现**,如实标注。

---

## 5. 移交结算

R8A 标为 **R8B** 的 7 条全部处理:**H-1 / H-2 / H-7 / H-14 / H-16 结清**,
**H-17 确认并收窄**,**H-13 拆开后整体移交 R8C**(逐份对齐 8 个平台就绪判定,
与 web 面重叠更大;判据已在 `notes/r8b-90` §3 给出)。

**H-18(约 50 条未复核候选)按任务书要求处理**:对 13 份 `r8a-raw-*` 底稿,
先定位各自"可疑缺陷清单"小节起始行,**只在该行之后**抽锚点,再按 R8B 文件集过滤
——**去重后落在 R8B 的锚点共 9 个**,逐条读过后**只有 1 条是真正针对 R8B 的缺陷主张**(D4),
主线复核**属实**,并补上了 R8A 没说的那一半:**它不报警是因为 `model_catalog` 在
`_OPEN_DICT_TOP_LEVEL_KEYS` 里**——**与 `personalities` 那条是同一个豁免,一次坑用户、一次坑维护者**。
**如实交代**:H-18 的主体是配置面残余,**本轮未复核,继续挂 R11**。

**一条制度性观察**:H-18 这类"我知道我没做完"的移交,最大价值不是清单本身,
而是**下一轮能用一次机械过滤就判定"这块欠账与我有没有关系"**——本轮做这件事只花了几分钟。
若 R8A 当初只写"还有一些没查",这几分钟会变成"要么全读一遍、要么假装不存在"。
**移交项带锚点的制度,本轮第一次收到利息。**

---

## 6. 台账报数

`scripts/verify_ledger.py` 三项校验全过(**第一项即 R8A 新增的"基线仓库干净"断言**):

```
OK baseline=863e31318 files=8530 total_lines=2608452
  L1: files=511 lines=479923
  L2: files=2183 lines=713923
  L3: files=1895 lines=602085
  L4: files=560 lines=55902
  LT: files=3381 lines=756619
  SUM == repo total: 2608452
```

本轮 50 个文件 `layer` L2→L1、`status` → `R8B-deep-read`。
L1 由 461 文件 / 436,384 行增至 **511 文件 / 479,923 行**。

---

## 7. 关卡报数

### 7.1 引用校验(`scripts/verify_citations.py`,全量输出)

```
citations=1335  OK=772  UNCHECKED=563
OK: every code-block-backed citation matches the baseline
```

**零 MISMATCH、零 MISSING-FILE。** 过程中修掉的问题分三类,值得逐类记:

1. **行号漂移 30 余处**,大部分由脚本 `--fix` 自动修正。其中**一条是真错而非漂移**:
   一段 `try/except` 被标到 `cli.py:7299`,而该行是同一函数里**另一个** `try`
   ——正文引的那段实际起于 **7318**。**这正是这道关卡存在的意义**:
   它看上去完全合理,不校验的话会一直被当成真的。
2. **12 处第三方库引用被打了基线 commit 标记**(`prompt_toolkit/...:N @ 863e313`)。
   `prompt_toolkit` 不在基线仓库里,它是 **3.0.52** 版的依赖,
   **拿基线 commit 给它的行号背书是错的**。已全部改写为
   `prompt_toolkit/....py 第 N 行 @ prompt_toolkit 3.0.52`,既保留信息又不再冒充基线引用。
3. **两处"实跑记录"被误当成源码块**(shell 命令与 grep 输出),已与锚点隔开并注明不参与校验。

### 7.2 可校验比例:**772 / 1335 = 57.8%,低于 R8A 定的 70% 下限**

**如实报告:本轮没达到这条下限**(R8A 实测 74.5%,R7C 31.7%)。
但本轮同时发现**这个指标本身有一个必须写下来的失真**,否则下一轮会读错它:

**该脚本只校验"锚点在前、代码块紧随其后"的引用。**
本轮有一份底稿(`r8b-raw-cli-session-model.md`)通篇采用**相反的排版约定**
——先贴代码块、锚点写在块**下面**。它有 48 个逐字代码块、证据密度并不低,
但初测 **180 条引用里只有 4 条可校验**。
主线写脚本把 45 处尾随锚点机械前移后,可校验数升到 **45**,
**并当场暴露 3 条此前完全不可见的真实行号错误**(已修)。

**结论有两条:**

- **这条指标衡量的是"排版是否合规",与"证据是否扎实"只是相关、不是等同。**
  一份把 `路径:行号` 大量用作**行内交叉引用**的底稿(本轮 `r8b-raw-subcommands.md`
  把 160 处短引用升级成完整锚点),分母涨了、分子没涨,**越认真反而分越低**。
- **但"锚点必须在代码块之前"这条排版约定是有真实价值的**,因为不合规的那份底稿
  藏了 3 条错误行号,只有转成合规格式才被抓出来。

**因此本轮的建议不是下调下限,而是把约定写死**(见 §9 第 5 条)。

### 7.3 测试作行为规格

| 套件 | 文件 | 通过 | 失败 |
|---|---|---|---|
| `tests/cli` | 105 | **926** | **0** |
| `tests/hermes_cli` | 551 | **4,345** | **5** |
| **合计** | **656** | **5,271** | **5** |

**venv 包数:87**(`[dev]` extra + `aiohttp 3.14.1` + `brotlicffi 1.2.0.1`)
——按 R8A 立的规矩记录,因为**用例数是环境的函数**。

**5 个失败全部逐个查到机制,全部为环境性质、非代码缺陷**,已并入 `CLAUDE.md`
的"已知环境限制"表供后续会话直接引用,不必重复排查:

| 用例 | 根因 | 机制 |
|---|---|---|
| `test_browser_connect_dual_stack::test_skips_occupied_successor` | 无 IPv6 | `find_free_debug_port` 要求 `127.0.0.1` **与** `::1` **两族都可绑**;无 IPv6 时每个候选都失败,函数走它自己文档化的兜底 `return preferred + 1` |
| `test_migrate_xai::test_apply_refuses_to_overwrite_unreadable_config` | 以 root 运行 | root 无视 `chmod 000`,不抛 `PermissionError` |
| `test_gateway_service`(systemd 单元) | 以 root 运行 | 被测代码自己拒绝:`Refusing to install the gateway system service as root` |
| `test_approvals_suggest::test_normalize_folds_home_prefix` | 以 root(`HOME=/root`) | `_home_prefix_fold_regex`(`tools/approval.py:1072 @ 863e313`)对"根下不足两段"的路径**故意返回 `None`**,防畸形 HOME 改写无关前缀;`/root` 只有一段 |
| `test_xai_provider_labels` | 无 models.dev 目录(离线) | 目录条目数实测 **0**、无本地缓存,`get_label` 回落原始 id,得 `'xai'` 而非 `'xAI'` |

**注意第四条的价值**:它不是"root 导致测试环境不对",而是**被测代码里一条有意的安全守卫**
——防止 `HOME=/` 这类畸形值把无关路径前缀改写掉。
**查清它花的时间,比标一句"root 环境问题"多不了多少,但结论完全不同。**

### 7.4 基线洁净(R8A 新增的第一项校验)

全程实测 `git -C /home/user/hermes-agent status --porcelain` 为空、`HEAD` 仍是 `863e31318`。
**8 个子代理全部遵守只读约束**;其中一个报告说测试运行器落了一个 gitignore 内的
`test_durations.json` 缓存并已自行删除——**主动报告这类痕迹的行为值得肯定并写进制度**。

---

## 8. 如实声明的边界

1. **H-18 主体未复核**(约 50 条中只处理了锚点落在 R8B 的 9 条),继续挂 R11。
2. **H-13 的 8 份平台就绪判定未逐份对齐**,整体移交 R8C——本轮只给了判据。
3. **H-14 给判据、未逐处落实**(8 处无保护调用点),记 H-R8B-b 移交 R11。
4. **■-R8B-07(审批 fail-open)未在运行时构造出触发场景**,当前无活体 bug;
   且 `tools/approval.py` 属 R3 范围,本轮**不认领该文件**,仅作跨范围观察记录。
5. **H-17 未在运行时复现**(§4.3)。

---

## 9. 下一轮建议

**下一轮做 R8C:dashboard 与 web 面**(26 文件 / 36,737 行,台账 `round=R8C`)。理由:

1. **本轮向 R8C 移交了两笔**(H-13 的 8 份对齐、H-3 的 `/api/pairing/approve` 鉴权层),
   而 R8A 还留了 H-10 / H-11 两笔也在 R8C,**四笔在同一面,趁上下文热做掉最划算**。
2. R8C 主体 `web_server.py` 17,732 行,是继 `cli.py` 之后的第二大文件,
   而本轮已在 `/whoami`、配对批准等处从 CLI 侧读到它的边缘。
3. **R8D 仍是 177 文件 / 125,634 行**,开轮时应按 R8A / R8B 同样方法先核一遍是否继续拆。

**打法建议**:

1. **段数压到 6-8 段这条,本轮验证有效,建议固定下来。** R8A 用 14 段跑到第 8 批被限流打断;
   本轮 8 段、分三批(3/3/2)、由主线直接派发独立子代理(**不走 workflow**),**全程未触上限**。
   **另一条同样重要:主线要自己扛一段正面精读**——本轮主线亲读入口链,
   既保证了成品章的主线叙事连贯,也让"子代理集体跑偏"不至于毁掉整轮。
2. **"前提写成可被否定的断言"应升级为强制格式。** 本轮 8 段 × 3 条前提,
   **被证伪的占压倒多数**,且每一次证伪都直接产出了本轮最好的材料
   (541 行的"构造函数"、因果颠倒的 resize、不在热路径上的状态栏函数)。
   建议此后子代理任务书**必须**包含 3 条可证伪前提,并明确要求"前提不成立就先报前提"。
3. **一条新观察,值得写进制度**:`tools/approval.py` 已是 `R3-deep-read`,
   而本轮从**调用方**读它时仍挖出 fail-open。
   **"文件已精读"与"该文件的所有契约已被检验"是两件事**——
   前者是按文件的覆盖,后者是按**契约**的覆盖。
   建议 **R11 复盘时专门查一类东西:跨越轮次边界的契约**
   (回调协议、字典形状、注册表 ↔ 分发链),它们按文件切分时**恰好落在缝里**。
4. **本轮头条的形状值得做成一次专项**:"两份必须一致的东西,中间只有人"
   在本轮拿到 **7 个实例**(3 个漏跟 + 4 份手工名单)。
   建议 R11 复盘时把 R2–R10 各轮的同类实例拉一张总表——
   它很可能是这个仓库**单一最高产的缺陷母题**。
