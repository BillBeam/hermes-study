# Round 8A 报告 · 配置面

**一句话结论:配置面学透,R8 切四片,定案 25 条。**

头条发现:**同一份 `config.yaml`,Hermes 用两个装载器读,合并语义不一样。**
主装载器递归深合并,`cli.py` 那个只做一层 `dict.update`。同一个 `HERMES_HOME`、
同一份只设了两个嵌套叶子键的配置文件,跑出来 `browser.camofox` 一边 **6 个键**、
另一边 **1 个键**——用户想打开一个布尔开关,在 CLI/TUI 这一侧**顺手删掉了同级的全部默认值**。
这个例子至今没炸,是因为受害键的默认值字面量**在仓库里存在三份**,读取点自带硬编码兜底。
**但"下一个没有兜底的键"不用等——本轮实跑抓到了它。**

`agent.reasoning_effort`(思考力度开关)是**真实被支持、真实被 CLI 读取**的键
(`cli.py:8166`),它在 `cli.py` 那份默认值里、**不在 `DEFAULT_CONFIG` 里**;
而 `hermes config set` 的键名校验**只认 `DEFAULT_CONFIG`**。实跑输出原样抄录:

```
✓ Set agent.reasoning_effort = high in .../config.yaml
⚠ 'agent.reasoning_effort' is not a recognized config key — it was saved anyway, but Hermes may not read it.
  Did you mean: agent.reasoning_overrides
```

**一条完全正确的命令,得到一句假警告,外加一个会把配置弄坏的建议**——
`agent.reasoning_overrides` 的默认值是 `{}`(字典),用户照做写成字符串后
思考力度**静默失效**,而原本正确的那行已被删掉。
**头条由此从"结构性隐患"坐实为"已发生的用户可见故障"。**

**而且不是孤例。** AST 展开 `cli.py` 那份默认值得 89 键,与 `DEFAULT_CONFIG` 求差,
**28 个只存在于 CLI 一侧**(15 个是 `agent.personalities` 及其 14 个人格文本,13 个是真开关);抽 6 个实跑,
**5 个复现同样的假警告**。已确证其中 **2 个**(`agent.reasoning_effort`、
`agent.system_prompt`)既真被读、又收到假警告;其余未逐个确证,连同名单移交 R8B。

**最后一层最严重:重复的默认值会让别处正确的抽象失效。**
`tools/clarify_gateway.py` 有个解析澄清超时的函数,docstring 自称
"Single source of truth shared by every surface (messaging gateway, CLI, TUI/desktop)
so the timeout can't drift between them",解析顺序是**遗留键 `clarify.timeout` 优先、
规范键 `agent.clarify_timeout` 兜底**——设计正确,前提是没设过遗留键时它不存在。
**而 `cli.py:523` 的第二份默认值把遗留键钉死为 120。** 实测同一份只设了
`agent.clarify_timeout: 900` 的配置:**网关面 900,CLI 面 120**,三个调用点两个传 `CLI_CONFIG`。
**一个为"防止各面漂移"而写的单一真源函数,在两个面之间漂了 780 秒;
用户设的规范键在 CLI 面根本没有生效路径。** 函数本身一行没写错。
失效点距病根有三跳(第二份默认值 → 遗留键恒存在 → 优先级恒走遗留分支),
现场几乎不可能反推回去。

**对照组同样重要(否则会被误读成"这仓库到处都坏")**:同一形状在 `terminal` 上
**被正确处理了**——`cli.py:642` 明确让新键名 `backend` 压过遗留的 `env_type`,
实测有效。**差别在归一化写在哪一层**:`terminal` 那次写在 `load_cli_config` 内部
(重复默认值自己的家里),`clarify` 写在共享 helper 里,而它不知道有第二份默认值。
**教训因此比"别写两份默认值"更精确:凡依赖"这个键没被设过"的判断,
必须做在重复默认值所在的那一层。**

第二发现关乎"配置"这个词本身:**文档画的四级优先级链(CLI 参数 > `config.yaml` > `.env` >
默认值)在代码里不存在。** `config.yaml` 确实有一条统一合并链,但**环境变量根本不是其中一层**
——它只作为 `${VAR}` 的取值来源,以及被各消费点各自 `os.getenv`。而在后一种方式里,
环境变量**常常先被检查、赢过 `config.yaml`**,与文档排序正好相反。
更彻底的证据是:连读凭据的公共函数都有**两个方向相反的版本**(`get_env_value` 环境优先、
`get_env_value_prefer_dotenv` 文件优先),调用方自己挑。
R7C 移交的 QQBot 那个 bug 正长在这块无人区里——它躲开了配置系统的全部校验,
**因为它从来就不在配置系统里**。这一条本轮拿到了决定性证据:
Hermes 维护**两张** env 表(一张"运行时认识"、一张"向用户推荐"),
而这次改名在两张表里的落法**完全正确**——新名进两表并进文档,
旧名只留在"认识"表里、注释标明 `legacy aliases (pre-rename, still read for back-compat)`、
不推荐也不进文档。**整个仓库里凡走配置系统的地方都做对了,唯独 `status.py` 没跟上,
而它恰恰是直接读环境变量、绕过配置系统的那一段。**
于是这条定案从"某处笔误"升格为结构性结论:**配置系统的纪律,管不到不走配置系统的代码。**

**收尾时又推翻了自己一次,得到第三发现:两个系统之间的桥是双向的。**
`config.yaml` 里**每一个顶层标量键都会被灌进 `os.environ`**(`gateway/run.py:2058`,
仅当环境里没有同名变量)。这解释了一件否则很难理解的事——**为什么配置根层是开放世界、
不拒绝未知键**:那些键名是给技能脚本和外部程序当环境变量用的,**按定义无法穷举**。
连带更正:主线一度把 `_KNOWN_ROOT_KEYS` 当成"根键校验白名单",实际它**全仓只有一个使用点**,
且只用来给四个"看起来放错位置的 provider 字段"去重,**不拒绝任何未知根键**。
这条桥的判据是 `_key not in os.environ`,于是它是**第三处**与文档优先级相反的地方。
按此,头条那句"两个装载器"其实说少了:`config.yaml` 有**五个读取函数**
(`load_config`(含 `load_config_readonly`)/ `load_cli_config` / `read_raw_config` /
`read_user_config_raw` / 网关的 `_load_gateway_config`),**各有各的合并、缓存与优先级姿态**;
这条桥用的是 `read_user_config_raw`。
桥**必须**用原始读而不能用合并读,理由写在注释里,是一条很好的设计点:
**只有用户真正写下的键才可以被桥进环境**——若先合并默认值,
`DEFAULT_CONFIG` 的 82 个顶层键会**整体变成环境变量**。

---

## 0. 执行说明

- **实际执行模型**:运行时策略明确禁止把模型标识写入推送到仓库的产物(commit message /
  PR / 代码注释 / 任何仓库文件),因此**模型标识在本会话最后一条回复中给出,不写入本报告**。
  可写入的依据部分如实记录:标识来自运行环境系统提示的显式声明,**无独立第三方自证渠道**。
  这是本轮唯一一处对任务书字面要求的偏离,理由如上(与 R7B / R7C 同一处置)。
- **子代理探针(R7C 定为开轮第一步,本轮执行)**:开轮第一个动作就是发一个只读单文件探针
  (Read + Grep + Bash 各一次,目标 `hermes_cli/config_migrations.py`)。
  **三个工具调用全部成功**,返回的行数(685)、sha1、16 个顶层函数名与行号,
  与主线复核完全一致。**结论:子代理完全可用。**
- **本轮实际执行方式(如实记录,含一次中途受限)**:探针通过后铺开 **14 个精读子代理**
  覆盖全部 15 个文件。**注:本容器 4 核,子代理并发上限为 2**,14 个子代理只能分七批串行。
  **第一轮跑到第 8 批时触到会话级用量上限,7 个子代理直接报错终止**
  (错误原文 `You've hit your session limit`,非工具故障、非权限拦截);
  上限重置后用 `resumeFromRunId` 续跑,已完成的 7 段从缓存回放、只重跑失败段。
  另外两段(`tools-config-a/b`)正文已写完但**被截断在自验环节之前**,
  主线补跑引用关卡,修掉它们遗留的 5 处行号漂移。
- **主线承担的部分**:范围锚定、切片决策、专项交付物(配置项全表工具与三张表)、
  **逐条回源复核子代理的关键断言**、**九次运行时实验**,以及全部合成产出
  (主线卷 / 定案卷 / 测试卷 / 成品章 / 本报告)。
  受上述限流影响,**本轮主线独立取证的比重显著高于前几轮**——
  三条头条结论(两个装载器、四级优先级链不存在、双向桥)与
  ■-10 / ■-11 / ■-12 三条最重的缺陷,**全部由主线亲自跑代码得出,不依赖子代理**;
  子代理贡献的 ■-13 / ■-14 / ■-15 也都经主线回源复核(其中 ■-14 由主线独立复现)。
- **前置动作**:R7C 分支此前未合入 main。按先例开 PR #9 并 merge,再从 main 拉
  `claude/hermes-agent-r8a-config-fgbmnd` 开工。
- **测试环境**:按 `CLAUDE.md` 现有步骤一次重建成功,无新增问题。

---

## 1. R8 切片结果(任务书第一项)

台账 `round=R8` 桶实测 **268 文件 / 227,803 行**,是 R7C(28,282 行)的 8 倍。
按 R7C 报告 §10 的草案**逐条复核后重新划线**,判据是:
**哪些文件必须同时摆在眼前,一个机制才讲得清。**

| 子轮 | 主题 | 文件 | 行数 |
|---|---|---|---|
| **R8A** | **配置面 + 命令注册表**(本轮) | **15** | **21,893** |
| R8B | CLI 主干与子命令树 | 48 | 40,804 |
| R8C | dashboard 与 web 面 | 26 | 36,737 |
| R8D | 其余(kanban / update / proxy / observability…) | 179 | 128,369 |
| — | **合计** | **268** | **227,803** |

**合计与切前的 R8 桶逐位相等,`round=R8` 残留 0 条。** 切片以规则形式写进
`scripts/assign_layers.py`(非手改台账),重生成后 `path/kind/lines/status` 四列
与切前**逐字节相同**。

**R8A 相对 R7C 草案增了 5 个文件、没删任何文件**(逐条理由见 `notes/r8a-01` §1.3):
`env_loader.py`(没有它,"配置项全表"的读取点一列填不出来)、
`secret_prompt.py`(`config.py` 模块顶部直接导入)、
`status.py` / `pairing.py` / `subcommands/pairing.py`(三笔移交项的锚点文件)。

**没纳入 `profiles.py`**:读过它的 docstring 后判定它是**多实例隔离**(决定"读哪一份配置"),
不是配置解析的一层(决定"一个键取什么值"),留 R8B。
**没纳入 `web_server.py`**(17,732 行,R8C 主体):只读其中配对批准的那几处、不认领该文件。

---

## 2. 分层修订:15 个文件 L2 → L1

促升理由同 R6(8 个 memory backend)、R7C(9 个 cron 顶层 `.py`)先例:
**轮次主题本体留在 L2,与「本轮达成 L1 完成标准」直接冲突**。
规则落在 `assign_layers.py`,重生成 diff 恰好只有分层与轮次两列。

---

## 3. 主要学习成果(成品章 `chapters/r8a-configuration-surface.md`)

1. **"配置"是两套几乎不相交的系统**:一份有结构、有默认值、有 schema 版本、
   有深合并的 YAML 字典;和一堆没有任何统一规则、各读各的环境变量。
   把它们当成一件事,是理解这个配置面的头号障碍。
2. **解析失败的兜底方向是"保持现状",不是"回到出厂"。** YAML 坏了不退回默认值,
   而是沿用本进程上一次成功加载的配置——因为配置里有 `approvals.deny` 这类**限制性规则**,
   退回默认值等于用户手一抖把防线拆了。
3. **读时展开密钥,写时必须还原模板。** `hermes config set` 是"读整份 → 改一个键 → 写回",
   而"读"已经把 `${VAR}` 展开成明文。若无还原机制,改一个显示选项就会把 API 密钥
   明文写进 `config.yaml`。本轮**端到端跑通验证**了两种情形都如设计所愿。
4. **缓存键必须覆盖全部输入。** 光有文件 (mtime, size) 不够,还要记下"这次展开依赖的
   环境变量当时各是什么值"——否则一次早于 `.env` 加载的 `load_config()` 会把未展开的
   字面量钉死整个进程生命周期。**"重启就好了"是缓存键不完整的典型征兆。**
5. **迁移梯:表驱动 + 支持下限 + 初始版本号不推进。** 最后一条看着别扭,
   却是从 768 行 `if` 长梯重构时**为了可证明的等价性**刻意保留的语义,并有测试断言
   新旧输出逐字相同。**重构的正确性优先于重构的美观。**
6. **一份定义,多个各自过滤的视图。** 94 条命令定义派生出四个消费面,
   每个视图的过滤规则都是隐式的;"到底有多少条命令"必须先问"对谁而言"。
7. **"粗粒度集合 + 细粒度开关"不能靠展开后判断成员。** 工具集那边踩过这个坑:
   复合工具集展开后,"从没设过"和"设了又关掉"**长得一模一样**,于是用户关掉的工具集
   重启后自己回来了。修法是换判据(显式成员优先),不是补展开逻辑。
8. **自动修正外部输入必须出声。** 凭据的 ASCII 清洗是全章最贴近用户痛点的一段:
   从 PDF/网页复制的密钥混进 Unicode 同形字,HTTP 头发不出去,供应商只回一句
   "invalid API key"。Hermes 剥掉非 ASCII **并大声警告、给出病因与处方**——
   **静默修好等于把可诊断问题变成不可诊断问题。**
9. **同一个语义被实现不止一次,是本簇几乎所有缺陷的共同病根——本轮数出六例。**
   两个装载器 / 三份同一默认字面量 / 两份"顶层标量→环境"的桥 / 两个方向相反的凭据读取函数 /
   一份**已经漂移**的注释模板死副本 / **两个对"cua-driver 装没装好"给出相反答案的判定**
   (后者最刺眼:规范答案已存在、docstring 还声明了自己是规范答案,安装路径就是没用它)。
   **判据不是"两份一样吗",而是"它们会不会一起改"。**
10. **跨边界对齐用了会因无关原因变化的值。** 三条缺陷同形不同装:
   遗留键名(`clarify.timeout`)、"键在哪份默认值里"(`agent.personalities`)、
   以及**拿界面文案当匹配键**(`hermes tools` 的插件注入,而稳定键 `"web"` 就在同一个 dict 里)。
   **判据:问"这个值会因为什么原因改变";只要有一条理由与对齐关系无关,它就不能当键。**
11. **分级判据必须分趟扫描。** provider 选择器把"强信号:它是激活的"与
   "弱信号:key 配齐了"写进同一趟循环,弱信号在同一次迭代里短路,
   于是**列表顺序压过信号强度**——用户回车一下就把 provider 改回了旧的。

---

## 4. 定案(`notes/r8a-90`,25 条:▲ 4 / ◇ 6 / ■ 15,另驳回 1 条)

### 4.1 三笔 R7C 移交项全部结案

| 移交项 | 结论 |
|---|---|
| ① `status.py` QQBot 环境变量倒置 | ✅ **成立**,并**更正来源**、**追加一条** |
| ② `commands.py` 94 条注册表定义面 | ✅ **94 确认无误**,补齐五数关系 |
| ③ `pairing.py` / `web_server.py` 批准入口 | ✅ **两把钥匙都读了**,记两条缺陷 |

**①的更正与加强**:R7C 说"安装向导写新名"——实测新名的来源是**网关的弃用警告**
与**官方文档**(`website/docs` 中新名 3 次、旧名 0 次),不是安装向导。
**追加发现:那个兼容分支不只恒假,它的分支体还是空操作**——分支体读的是
`QQ_HOME_CHANNEL`,与三行之上主路径已经读过的是同一个变量。
**用户可复述的因果**:照官方文档配新名 → 机器人一切正常 → 但 `hermes status`
从此不再显示 home 频道。**面板惩罚了照做的人。**

**②的补齐**:94 个 `CommandDef` / 26 个别名 / 120 个可敲 token / 8 条 `gateway_only`
(带 1 别名)/ 对外扁平字典 111 条 —— 120 − 9 = 111,按类目再分加总也是 111,自洽。

**③的记录**:两把钥匙就是 `hermes_cli/pairing.py:72,74`(CLI)与
`hermes_cli/web_server.py:12337,12339`(dashboard),全仓再无第三处批准调用点。
二者**都**捅穿了 `PairingStore` 的私有成员,为的是把"码错了"和"你被锁定了"区分开——
这个张力是真的(对攻击者必须不可区分,对运维者必须可区分),**方向对、实现脆**,
缺的是一个明确标注"仅限已认证侧"的诊断 API。
**并且两把钥匙行为不一致**:dashboard 只在"码"这条路径上报锁定(并注释了理由),
CLI 两条路径都报。后果:运维者用一个**已过期的 request-id** 去批准、
而平台恰好因别的原因处于锁定期时,CLI 会让他去等一个与他的问题无关的计时器,
dashboard 则正确回 404。

### 4.2 ▲ 组:地图与代码冲突(4 条)

- **▲-1** 代码用 `docs/design/managed-scope.md §4.1` 给最反直觉的优先级规则标出处,
  **该文件不存在**;但内容其实在 `website/docs/user-guide/managed-scope.md` 里写得很准确。
  **性质是指针失效,不是内容缺失**——不写清这一点就是夸大。
- **▲-2** 文档把 `bedrock.discovery` 的三个键讲成可调,**三个键零读取点**:
  TTL 是模块级常量,`provider_filter` 是**从没有人传的函数参数**。
  **毒性在于常量 3600 恰好等于文档里的默认值**,照文档配一遍会得出"生效了"的结论。
  同一 `bedrock:` 段下的 `region` 与 `guardrail.*` **是接了线的**,肉眼分辨不出差别。
- **▲-3** 文档的四级优先级链在代码里不存在(见头条二)。
- **▲-4** QQBot 变量名(即移交项①)。

### 4.3 ◇ 组:文档缺口(6 条)

- **◇-1** 856 个配置键里 **105 个在全部文档面上零提及**。
- **◇-1b** `DEFAULT_CONFIG` 并非全部合法配置键:另有 **23 个根键**合法但故意不在默认值里
  (`mcp_servers` / `platforms` / `platform_toolsets` / `image_gen` / `video_gen` 等),
  它们**没有默认值也没有嵌套键定义**,其子树在全表中零覆盖;其中 **6 个零文档**。
  更进一步,根层**根本不做闭世界校验**(见 ◇-1c),所以"合法配置键"这个集合是开放的。
- **◇-1c** **第三条桥零文档**:`config.yaml` 顶层标量 → `os.environ`
  (`gateway/run.py:2058`)。这是配置与环境之间**反方向**的通道,
  也是根层开放世界的原因;`website/docs` 的配置文档全文未提。
- **◇-1d** **第三类表外键(嵌套层)**:`TERMINAL_CONFIG_ENV_MAP`(`config.py:3183`,30 键)
  与 `DEFAULT_CONFIG["terminal"]` **双向不同构**——8 个桥表键无默认值定义,
  6 个默认值键不在桥表里(设了也传不到 `terminal_tool`)。
- **◇-2** 对照组:151 条静态环境变量**零缺口**。
- **◇-3** 两个装载器的存在本身全站零文档(实为**五个读取方**)。

### 4.4 ■ 组:代码内部缺陷(15 条,只记录不修)

QQBot 双重死代码 / `bedrock.discovery` 三键未接线 / 两个装载器合并语义不同 /
`display.copy_shortcut` 全仓仅一次出现(其注释还列了四个合法取值)/
`NOUS_BASE_URL` 被"缺失变量"盘点主动索要而无人读(**且违反了本仓库自己写下的"废弃键从推荐表摘除、在认识表保留"规则**)/
配对 CLI 捅穿封装 / `OPTIONAL_ENV_VARS` 在 import 时被原地改写 /
**两份默认值的第一个真实受害者**(`agent.reasoning_effort` 被判未知键 + 错误建议)/
**"单一真源"的澄清超时函数在两个面上相差 780 秒**(遗留键被第二份默认值钉死)/
**14 个内置人格在网关侧完全不存在**(CLI 有 14 个,聊天里 `/personality` 回"none configured")/
**插件注入拿类目界面文案当匹配键**(稳定键 `"web"` 就在旁边;`web` / `video_gen` 两类目无硬编码行,改文案即整类目静默变空)/
**两级优先级写成一趟循环**(provider 选择器光标停在"留着旧 key"的行上,回车即静默改回旧 provider,主线独立复现)/
**同一进程两个界面对"cua-driver 装好没有"给出相反答案**(安装判定用裸 PATH,就绪判定用规范解析器)/
**两把配对钥匙行为不一致**(CLI 在 request-id 路径上也报"平台被锁定",dashboard 有此限定而 CLI 没有)/
`_COMMENTED_SECTIONS` 是**已经漂移的死副本**(活版是 `_SECURITY_COMMENT` / `_FALLBACK_COMMENT`,
同一句话两份已不同;维护者最容易改到的恰是那份不生效的)。

### 4.5 主线驳回子代理结论 1 条(复核制度的直接产出)

`notes/r8a-raw-config-c` 的 **F4** 断言"坏 YAML 会被 `hermes config set` 静默截断",
论据是写前守卫只做 `f.read(1)`。**前提属实,结论不成立。**
主线构造 13 行含 `approvals.deny` 的坏 YAML 实跑:命令**拒绝执行、打印具体解析错误、
退出 1、文件逐字节未改**。原因是 `set_config_value` 不走 `read_raw_config()`,
它**自己再解析一次**并硬退出,`unset_config_value` 同样有一份。
**教训:共用守卫弱 ≠ 整条路径弱,判定写路径必须读调用方的完整序列。**
残留疑问(是否存在第三个直接 `read_raw_config()` 后落盘的调用方)作为 **H-7** 移交。

---

## 5. 专项交付物:配置项全表(任务书指定)

`scripts/config_table.py` 从 `DEFAULT_CONFIG` / `OPTIONAL_ENV_VARS` / `_EXTRA_KNOWN_ROOT_KEYS`
的**字面量 AST** 抽取(不 import、不执行),对任意 commit 可重跑。三张表:

| 文件 | 内容 |
|---|---|
| `data/r8a-config-keys.tsv` | **856 个配置键**:键名 / 类型 / 默认值 / 定义处 / **Python 读取点数 + 样例** / **TypeScript 读取点数 + 样例** / 叶名歧义标记 / 文档覆盖面 |
| `data/r8a-env-vars.tsv` | **151 条静态环境变量**:同构字段 + 官方描述 |
| `data/r8a-extra-root-keys.tsv` | **23 个**不在 `DEFAULT_CONFIG` 里但合法的根键 |

**读取点按语言分列不是装饰,是本轮一条方法论教训换来的。** 第一版脚本只扫 `*.py`,
得出"8 个键零读取点"。逐条手工复核**推翻了其中 5 条**——它们不是死键,
而是**根本不由 Python 读**:Python 把合并好的配置经 `config.get` RPC 发出去,
真正解释这些键的是 TypeScript 客户端(`ui-tui/` / `web/` / `apps/desktop/`)。
**任何只扫单一语言的"谁读这个键"分析,对这一大类键结构性失明。**

**另一条自我更正**:本轮原想给出"文档覆盖率百分比",最后**放弃了**。
按点分全路径匹配得 **0.0%**(文档写 YAML 块,不用点分写法,这个数量的是排版习惯);
按叶子名匹配得 87.7%(而 `enabled` / `timeout` 这类叶名满地都是,严重高估)。
**两个边界都不可用,所以报告只报那个确定成立的数:105 个键零提及。**
报一个自己都不信的百分比,比不报更糟。

**表的适用边界(已写进脚本开头,请务必先读)——本轮把它收窄了三次:**

1. **`DEFAULT_CONFIG` 侧是可信全集**(AST 与运行时逐位相等:856 / 137 / 719 / 82 根),
   但**"有默认值的键" ≠ "用户能合法写的键"**。表外还有三类:
   23 个额外根键**及其整棵子树**;只存在于投影表里的嵌套键
   (如 `TERMINAL_CONFIG_ENV_MAP` 30 键中的 8 个);以及**任意顶层标量**
   (根层是**开放世界**,因为这些名字会被桥进 `os.environ`)。
2. **环境变量侧只有 49%** —— `OPTIONAL_ENV_VARS` **在 import 时被原地改写**,
   151 条涨到 **308** 条。
3. **而且还有第二张 env 注册表**:`_EXTRA_ENV_KEYS`(108 条),
   装"运行时认识但不该推荐"的键。**"认识的"是两表并集(静态 239 / 运行时 365),
   "推荐的"只是 `OPTIONAL_ENV_VARS`。** 哪一张单独拿出来都回答不了
   "这个程序认识哪些环境变量"。

**这三次收窄本身就是本轮的一个产出**:一张看起来很全的表最危险的地方,
是它**不声明自己不全**。

---

## 6. 测试作为行为规格(`notes/r8a-95`)

规格集用两条窄判据取并集(而非"import 了 `hermes_cli.config`"——那会拉进 399 个文件、
毫无区分度),得 **170 个文件**。

**结果:170 文件 / 3,183 用例全部通过,0 失败,76.1 秒。** 本轮规格集不含
R7B/R7C 遇到的 IPv6 用例,故零环境性失败。

三条值得记的观察:

1. **QQBot 死分支能活下来,不需要一个骗人的测试,只需要没有测试。**
   `test_status.py` 的 10 个用例里五支是**抗崩溃测试**(断言不抛异常),
   没有一支断言平台表那一行的内容;全仓提到 `QQ_HOME_CHANNEL` 的测试只有一处,
   还是在 `monkeypatch.delenv` 的清理列表里。
   **判据:"向后兼容"分支 + 模块测试全是抗崩溃测试 ⇒ 该分支极可能从未执行过。**
2. **两个装载器的分歧比"没测"更微妙:这条性质被测了,而且有一支以它命名的用例
   (`test_cli_config_managed_leaf_preserves_user_siblings`)——但它测在了唯一做对了的
   那一层(managed 覆盖)上。** 出问题的"默认值 ← 用户配置"那一步,
   用户配置永远是被覆盖的基底、从来不是覆盖方,所以浅合并路径在这支专测同级键存活的
   用例里也没被走到。
3. **迁移那边有两支真正的不变量测试**:一支断言注册表里不残留低于支持下限的步骤,
   一支断言**砍掉旧步骤后受支持区间的输出逐字不变**——把"这次删代码没改变行为"
   变成了可执行断言。

**本轮范围内"文档即测试"检索结果为零**,与 ◇ 条目的分布吻合。

---

## 7. 台账报数

`scripts/verify_ledger.py` 三项全过:

```
OK baseline=863e31318 files=8530 total_lines=2608452
  L1: files=461 lines=436384
  L2: files=2233 lines=757462
  L3: files=1895 lines=602085
  L4: files=560 lines=55902
  LT: files=3381 lines=756619
  SUM == repo total: 2608452
```

本轮 `status`:**15 个 `round=R8A` 文件 → `R8A-deep-read`**,diff 恰好 15 行改动。
L1 由 446 文件 / 414,491 行增至 **461 文件 / 436,384 行**,增量 **15 文件 / 21,893 行**,
恰为本轮促升量,L2 同额减少,总行数不变。

---

## 8. 引用关卡报数(本轮起为定稿关卡)

见 §9 制度升级一。**全部产出定稿前全量运行 `scripts/verify_citations.py` 并通过。**

（最终数字见文末"定稿关卡最终运行"。）

---

## 9. 制度升级两条(任务书指定,已写入 `CLAUDE.md`)

**升级一 · `scripts/verify_citations.py` 升格为定稿关卡。**
写进「验收标准」,与台账校验并列:每轮 commit 前对本轮全部 `notes/` 与 `chapters/`
全量运行,**跑到退出码 0 才算过关**,并在报告里报数;`--fix` 只用于无歧义漂移,
**用后必须裸跑复核**。证据格式从人工约定升级为**脚本可校验**。

**本轮实战效果值得单独说**:这道关**先抓住的是主线自己**——
主线卷首次运行报 8 处行号漂移(全部出自主线亲手写的引用),
成品章 1 处,定案卷两轮共 4 处。**若无此关,这 13 处会直接进入 R12 蓝图的正文。**
R7C 建议升格时的理由("它抓到的 60 余处里有 3 处出自主线")在本轮再次成立,
且比例更高。

**升级二 · 移交项格式。** 写进「验收标准」:凡向后续轮移交的未决项,
**必须附「锚点文件 + 一句话现象」**。理由(R7C 实测)一并写入:
R7 有一条移交项因只留标题被下一轮判错定位,另一条被判宽范围;
R7B 更有一条只在"下一轮建议"里出现过标题、从未取证,却被当成已取证结论传下去。
**本轮移交的 8 条全部按新格式给出锚点文件(带行号)与一句话现象。**

---

## 10. L1 完成标准自评

对簇内每个机制能讲清"解决什么问题 / 怎么实现 / 为什么这么设计 / 有什么取舍"(成品章即证),
每篇底稿附「重实现要点」。

**本轮的特点是主线独立取证比重高**,且**四次更正了自己**:
(a) 第一版读取点分析只扫 Python,把 5 个 TS 消费的活键判成死键;
(b) 想给文档覆盖率百分比,发现两个边界都不可用后撤回;
(c) 手工数 `_EXTRA_KNOWN_ROOT_KEYS` 数成 24,AST 复核为 23;
(d) 把 `_KNOWN_ROOT_KEYS` 误读成"根键校验白名单",复核后发现它**全仓只有一个使用点**、
    **不拒绝任何未知根键**——而这次更正直接牵出了本轮第三发现(双向桥与开放世界根层)。
**另驳回子代理结论 1 条(F4)。**

**四次更正里有三次是"我先给了一个数/一个判断,再去验证它"时暴露的。**
这是本轮方法上最值得留给后续轮的一条:**先把结论写成可被自己证伪的形式
(一个具体的数、一个具体的集合),再去验它**——含糊的结论不会暴露错误。

**如实声明的边界**:

1. **两个装载器分歧的"实际影响面"未穷举。** 已证明分歧存在(运行时实测)、
   已证明受害样例被硬编码兜底掩盖;但**哪些从 `CLI_CONFIG` 读的嵌套键没有兜底**
   ——即真正会出事的集合——本轮未逐个排查,作为 **H-2** 移交 R8B。
2. **`mcp_servers` / `platforms` 等 23 个额外根键的子树未展开**,
   配置项全表对它们零覆盖(◇-1b 已显式标注,不是隐藏的洞)。
3. **`hermes_cli/managed_scope.py` 本体未读**——本轮从 `config.py` 与 `cli.py` 两侧
   读到它,但没读实现,作为 **H-4** 移交。

---

## 11. 下一轮建议

**下一轮做 R8B:CLI 主干与子命令树**(48 文件 / 40,804 行,台账 `round=R8B`)。理由:

1. **本轮欠的两笔账都落在 R8B**:`cli.py:441` 那份**第二默认值字面量**
   (11 个顶层键,其中 `clarify` 在 `DEFAULT_CONFIG` 里不存在)需查清 CLI 专属键还有哪些;
   `cli.py:599` 的浅合并**实际影响面**需穷举(H-1 / H-2)。
   这两笔是本轮头条结论的直接延伸,拖到 R8D 就断了。
2. **`cli.py` 18,555 行是全仓第一大文件**,而本轮只读了它的配置装载部分。
   它同时是 R8A 头条的另一半,趁上下文还热做掉最划算。
3. R8C(web 面)与 R8D(其余)彼此独立,顺序可后置;
   **R8D 179 文件 / 128,369 行仍偏大**,开轮时应按本轮同样方法先核一遍是否继续拆。

**打法建议**:

1. **子代理开轮探针继续保留**(本轮一次通过,成本约一分钟)。
   但本轮暴露了探针查不出来的两个约束,**下一轮开轮必须一并核**:
   **(a) 并发上限**——本容器 4 核 ⇒ 上限 2,14 个子代理串成七批,墙钟是主要成本;
   **(b) 会话级用量上限**——本轮跑到第 8 批时 7 个子代理**同时被限流打断**。
   探针只能证明"工具可用",证明不了"额度够跑完 14 段"。
   **建议 R8B 起把段数压到 6-8 段(每段更粗)**,或主线承担更多正面精读:
   段数越多,越可能在中途撞上限,而**中途失败的代价不是线性的**——
   本轮有两段正文写完却被截断在自验之前,遗留 5 处行号漂移要主线补收。
   另:`resumeFromRunId` **确实按预期工作**(已完成段从缓存回放),这条路子可靠,
   值得作为标准做法写进打法。
2. **把"可校验比例"作为一个常规指标报出来。** 该脚本只逐字校验**带代码块的**引用,
   散文引用记 UNCHECKED。本轮实测 **792 / 967 = 81.9%** 带代码块,
   而 R7C 是 **802 / 2,531 = 31.7%**。差别不是本轮更严谨,而是**产出结构不同**:
   本轮底稿在写作时就把"断言 + 原文块"当默认格式。
   **建议 R8B 起把这个比例写进报告,并以 80% 为下限**——它比"跑没跑过关卡"信息量大得多:
   关卡只能证明"写下的行号是对的",这个比例才反映"有多少结论是真被逐字锚定的"。
3. **配置项全表已是资产,R8B 可直接消费**:查 `cli.py` 专属键时,
   拿 `data/r8a-config-keys.tsv` 做差集即可,不必重新盘点。
4. **一个跨轮提醒**:本轮驳回了一条子代理结论、更正了四处自己的结论。
   建议 **R11 复盘时把 R2–R10 各轮"被驳回/被更正"的条目单独拉一张表**——
   它们比"已确认"的条目更能说明这套流程的失效模式。
